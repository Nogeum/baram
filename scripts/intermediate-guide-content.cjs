const P=t=>({type:'p',text:t}),H=t=>({type:'h',text:t}),L=(...items)=>({type:'list',items}),T=(headers,rows)=>({type:'table',headers,rows}),C=(file,needle,count,points)=>({type:'code',file,needle,count,points}),N=(label,text)=>({type:'note',label,text}),Q=(question,answer)=>({type:'question',question,answer}),X=(title,task,checks,hint)=>({type:'exercise',title,task,checks,hint}),F=(...items)=>({type:'flow',items});
const J='src/main/java/org/example/portal/',R='src/main/resources/';
module.exports=[
{id:'orientation',title:'01. 문법에서 애플리케이션 설계로 넘어가기',goal:'메서드 단위의 코드 읽기를 요청·데이터·불변조건 단위의 분석으로 확장합니다.',blocks:[
P('이 문서는 Java의 클래스·컬렉션·예외와 HTML/CSS/JavaScript의 기본 문법을 알고 있다는 전제로 씁니다. 아직 Spring MVC·JPA·트랜잭션에 익숙하지 않아도 읽을 수 있도록 개념을 실제 코드에 연결합니다. if나 for의 뜻보다 “이 검사는 어느 계층이 책임져야 하는가”, “동시에 호출되어도 규칙이 유지되는가”에 집중합니다.'),
N('문서 범위','현재 프로젝트의 소스 스냅샷을 분석한 학습서입니다. 실제 개발 당시의 전체 순서나 팀별 기여를 재구성하지 않습니다. ‘현재 구현’, ‘분석’, ‘개선 실습’을 구분하며, 개선 예제는 앱에 적용하지 않았습니다.'),
T(['읽을 때의 질문','이 프로젝트의 예'],[['입력과 신뢰 경계는 어디인가?','client의 employeeId 대신 Principal로 현재 직원을 결정'],['반드시 유지할 규칙은 무엇인가?','직원당 하루 출근은 최대 한 건'],['원자적으로 묶을 변경은 무엇인가?','정정 신청 승인 + 실제 출퇴근 수정 + 알림'],['조회 결과를 어디까지 노출하는가?','부서 캘린더는 업무 요약만, 상세는 본인만'],['실패와 재시도를 어떻게 처리하는가?','채팅 UUID를 재사용해 저장 중복 방지'],['어떻게 확인할 수 있는가?','서비스 테스트·HTTP 보안 테스트·브라우저 검증을 구분']]),
P('읽는 순서는 구조 → HTTP → 보안 → 영속성 → 동시성 → 업무 상태 → 비동기 UI → 검증 → 운영입니다. 모든 장 끝의 질문에 코드 위치를 근거로 답해 보고, 실습은 별도 H2 환경에서 진행하세요. 이 HTML의 시뮬레이터는 실제 서버와 통신하지 않습니다.'),
Q('이 수준에서 “코드를 이해했다”는 기준은 무엇일까요?','정상 흐름을 설명하는 것에 더해, 입력을 바꿔 우회할 수 있는지, 동시에 실행되면 무엇이 경쟁하는지, 중간 실패 후 어떤 상태가 남는지, 그 주장을 검증할 테스트를 설명할 수 있어야 합니다.')]},
{id:'architecture',title:'02. 계층 구조를 실제 의존성으로 읽기',goal:'Controller·Service·Repository·Entity·Template의 경계를 파악합니다.',blocks:[
F('HTTP / Security Filter','Controller / Model·DTO','Service / 정책·트랜잭션','Repository / 조회·잠금','JPA / Oracle'),
P('이 앱은 Spring MVC + Thymeleaf의 서버 렌더링 구조입니다. 일반 페이지는 Model에 데이터를 넣고 템플릿 이름을 반환합니다. 채팅은 JSON API를 따로 두고 fetch로 화면 일부만 갱신합니다. HTML과 API가 공존한다고 프런트·백엔드가 완전히 분리된 SPA 구조가 되는 것은 아닙니다.'),
C(J+'web/GroupwareController.java','@ModelAttribute void common',9,['@ModelAttribute는 이 컨트롤러의 화면에서 공통으로 사용할 사용자·알림 정보를 준비합니다.','page는 화면 식별자와 제목을 설정하고 공통 portal 템플릿을 반환합니다.','assignees는 본인만 선택하게 하지만, 이것은 UX 제한입니다. Service도 동일 정책을 검사해야 합니다.']),
P('일부 Controller는 Repository를 직접 읽습니다. 그러므로 “모든 요청이 Service를 경유한다”는 그림은 정확한 코드 설명이 아닙니다. 쓰기 규칙은 Service에 집중되어 있지만 조회 모델 조립은 Controller와 Service에 나뉘어 있습니다. 리팩터링은 이상적인 계층 수를 맞추기보다 중복된 정책과 쿼리 책임을 줄이는 방향으로 판단합니다.'),
N('분석','GroupwareService는 업무·결재·예약·자료실까지 여러 기능을 담당합니다. 현재 크기에서는 탐색이 쉽지만 기능별 변경 이유가 늘어납니다. TaskService, ApprovalService, ReservationService 등으로 나누는 것은 개선 후보이며 현재 구현이라고 설명하지 않습니다.'),
X('의존성 지도 그리기','출근, 전자결재, 사진 업로드의 진입점부터 저장소까지 실제 호출 파일을 표시하세요.',['조회 전용 호출과 변경 호출을 구분한다.','보안 필터와 서비스 정책 검사를 따로 표시한다.','현재 Controller의 직접 Repository 조회를 숨기지 않는다.'],'PortalController.checkIn, GroupwareController.createDocument, ProfileImageController.upload에서 시작하세요.') ]},
{id:'http',title:'03. HTTP 계약: 화면 응답과 JSON 응답을 구분하기',goal:'URL·메서드·요청 데이터·응답 형식·오류 처리를 하나의 계약으로 읽습니다.',blocks:[
C(J+'web/PortalController.java','@PostMapping("/employee/check-in")',1,['Principal에서 얻은 로그인 ID가 출근의 주체입니다.','결과를 HTML로 직접 반환하지 않고 done을 통해 redirect합니다.']),
P('일반 폼은 POST 처리 후 redirect하고 GET으로 화면을 다시 읽는 PRG 패턴을 사용합니다. 성공 안내는 RedirectAttributes의 flash에 넣습니다. PRG는 새로고침으로 인한 반복 제출 가능성을 낮추지만 동시 요청이나 네트워크 재시도를 해결하는 중복 방지 장치는 아닙니다.'),
C(J+'web/ChatController.java','@RestController',13,['@RestController는 엔티티 또는 DTO 반환을 응답 본문으로 직렬화합니다.','대화 상대 id는 경로에서 받지만 발신자는 인증된 Principal에서 얻습니다.','read는 204 응답입니다. JSON이 없으므로 클라이언트에서 무조건 response.json()을 호출하면 안 됩니다.']),
T(['계약 요소','검토 포인트'],[['GET /threads/{id}','조회 자체로 readAt을 변경하지 않는다.'],['POST /threads/{id}','content와 clientId를 검사하고 Message DTO를 반환한다.'],['POST /threads/{id}/read','수신자·상대·메시지 ID 조건이 맞는 행만 수정한다.'],['HTTP 오류','MVC 오류 페이지와 JSON 오류를 구분한다.'],['리다이렉트된 로그인','API fetch가 HTML을 JSON으로 오해하지 않도록 검사한다.']]),
X('채팅 API 명세 작성','현재 컨트롤러의 5개 엔드포인트를 요청·성공 응답·실패 조건으로 정리하세요.',['인증과 CSRF 필요 여부를 따로 적는다.','before가 상대 ID가 아닌 이전 메시지 기준임을 구분한다.','조회할 수 없는 데이터 범위를 명시한다.'],'ChatController와 ChatService를 함께 읽어야 계약이 완성됩니다.') ]},
{id:'authorization',title:'04. URL 권한과 객체 접근 권한은 별개입니다',goal:'역할·소속·소유권·상태를 조합해 접근 결정을 추적합니다.',blocks:[
C(J+'config/SecurityConfig.java','return http.authorizeHttpRequests',11,['hasRole은 주소군의 1차 필터입니다. /manager/**의 EMPLOYEE만으로 부서 관리 자격을 보장하지 않습니다.','anyRequest().authenticated()는 로그인 필요 조건일 뿐 해당 객체의 소유권까지 판단하지 않습니다.']),
C(J+'service/DepartmentAccess.java','public boolean canReview',5,['eligibleApprover가 같은 부서·유효한 관리자·자기 결재 제외를 담당합니다.','선택자가 자격을 잃은 경우, 내가 지정자인 경우, 본부장이 하위 결재자 업무를 처리하는 경우를 분기합니다.']),
P('개인 업무는 canTask, 전자결재는 canDocument와 canReview, 자료실은 canLibrary, 첨부는 fileAccess가 담당합니다. 같은 ADMIN이라도 자료실에서는 예외 열람 권한이 있고 개인 업무·전자결재에서는 제외됩니다. 관리자 우회 여부를 모든 서비스에 일괄 적용하면 현재 정책을 깨뜨립니다.'),
T(['대상','주요 판단 재료'],[['개인 업무·댓글','웹 관리자 아님 + 담당자가 본인'],['부서 결재','활성 직원 + 관리 권한 + 같은 부서 + 지정/대리 조건'],['전자결재 문서','작성자 또는 지정 결재선 참여자'],['프로필 변경','본인 또는 웹 관리자'],['채팅 기록','로그인한 나가 발신자 또는 수신자']]),
Q('직원 선택 셀렉트를 본인만 보여주면 충분할까요?','아닙니다. HTTP body를 바꿔 다른 ID를 보낼 수 있습니다. saveTask의 same(a,target) 같은 서버 규칙이 핵심입니다. 테스트도 UI 옵션 개수보다 직접 조작한 요청이 거부되는지를 확인해야 합니다.'),
X('권한 표 기반 테스트','직원 A·같은 부서 관리자 B·다른 부서 관리자 C·웹 관리자 D의 접근 표를 작성하세요.',['개인 업무 상세, 부서 승인, 채팅 조회를 별도로 검증한다.','자기 신청 승인과 삭제·비활성 계정도 포함한다.','403과 BusinessException 기반 응답의 실제 차이를 확인한다.'],'허용/거부만 추측하지 말고 각 경로가 어떤 예외 처리기를 사용하는지 확인하세요.') ]},
{id:'identity',title:'05. 세션과 계정 상태가 요청의 주체를 결정합니다',goal:'로그인 성공 뒤에도 반복되는 계정 검사의 이유를 이해합니다.',blocks:[
C(J+'service/PortalService.java','public Employee current',5,['Principal의 이름으로 DB 계정을 다시 찾아 현재 상태를 확인합니다.','로그인 당시 활성 상태였더라도 나중에 삭제·비활성화됐을 수 있습니다.']),
P('Spring Security의 UserDetailsService는 인증 시 저장된 비밀번호 해시와 역할·disabled 상태를 제공합니다. 일반 요청에서 서비스의 current 또는 actor를 거치는 것은 DB의 현재 유효성을 다시 확인하는 목적도 있습니다. 인증된 세션이라는 사실과 현재 업무 수행 자격이 같다고 가정하지 않는 것입니다.'),
C(J+'service/PortalService.java','@Transactional public String deleteEmployee',17,['마지막 관리자 보호를 위해 관리자 집합을 잠근 뒤 대상 계정 삭제 조건을 확인합니다.','삭제 상태·활성·권한·비밀번호를 바꾸고 관련 대기 신청도 정리합니다.','반환된 로그인 ID는 컨트롤러의 세션 만료 처리에 사용됩니다.']),
N('분석','논리 삭제는 모든 과거 상태를 완벽하게 재현하는 이력 모델과 다릅니다. 직원의 현재 부서 문자열만 저장하므로 부서 이동 전 시점의 조직 소속을 조회할 수 있는 별도 이력은 없습니다.'),
Q('비밀번호 환경변수를 바꾸면 기존 관리자 암호가 바뀌나요?','현재 BootstrapData는 직원 테이블이 비어 있을 때만 초기 계정을 만듭니다. 기존 계정의 비밀번호를 시작할 때 덮어쓰지 않습니다. 환경변수와 DB에 저장된 계정 상태를 구분해야 합니다.') ]},
{id:'jpa',title:'06. JPA: 객체 변경, flush, commit을 구분하기',goal:'영속성 컨텍스트와 트랜잭션의 실제 효과를 코드로 설명합니다.',blocks:[
C(J+'service/PortalService.java','@Transactional public void checkOut',7,['DB에서 조회한 Attendance가 트랜잭션 안에서 관리되는 상태라면 setter 변경을 JPA가 추적합니다.','명시적인 save가 없다는 이유만으로 퇴근이 저장되지 않는다고 판단하면 안 됩니다.']),
P('영속성 컨텍스트는 엔티티의 상태를 관리하는 단위입니다. 관리 중인 객체 변경은 flush 시점에 SQL로 동기화될 수 있습니다. flush는 DB에 SQL을 보내는 시점이고 commit은 트랜잭션을 최종 확정하는 시점입니다. saveAndFlush 뒤 예외가 발생하면 같은 트랜잭션의 변경이 롤백될 수 있습니다.'),
C(J+'domain/BaseEntity.java','@MappedSuperclass',7,['공통 시퀀스는 여러 테이블의 ID를 생성합니다. 연속 번호나 업무 건수로 해석하지 않습니다.','@Version은 엔티티 수정 충돌 검사에 사용되며 업무 신청 당시 원본 버전 스냅샷과 역할을 구분해야 합니다.']),
P('spring.jpa.open-in-view=false이므로 뷰까지 영속성 컨텍스트가 열려 있다는 가정으로 lazy 관계에 접근하면 안 됩니다. 현재 ManyToOne은 명시적인 LAZY를 지정하지 않은 곳이 많아 기본 EAGER에 의존하기도 합니다. EAGER는 필요한 화면이 쉽게 만들어지는 대신 불필요한 조회·N+1 가능성을 검토해야 합니다. 실제 쿼리 수를 측정하기 전 모든 경로에서 N+1이 발생한다고 단정하지 않습니다.'),
X('조회와 변경 감지 관찰','H2 테스트에서 Attendance를 저장·조회·수정하고 flush 전후 SQL을 비교하세요.',['관리 객체 변경과 detach 후 변경의 차이를 설명한다.','flush 뒤 예외가 났을 때 DB 최종 상태를 확인한다.','로그에 비밀번호나 개인정보를 불필요하게 출력하지 않는다.'],'별도 테스트 트랜잭션과 EntityManager.clear를 이용하면 1차 캐시 때문에 생기는 착시를 줄일 수 있습니다.') ]},
{id:'boundaries',title:'07. 트랜잭션 경계는 업무의 원자성을 표현합니다',goal:'서비스 메서드의 경계·프록시·롤백을 검토합니다.',blocks:[
C(J+'service/AmendmentService.java','if(approve){',8,['원본 버전을 확인하고 undo와 replace를 같은 승인 흐름에서 실행합니다.','새 신청 생성이 실패하면 원본 취소만 확정되지 않도록 전체 변경이 묶여 있어야 합니다.']),
P('@Transactional의 기본 전파는 일반적으로 REQUIRED입니다. 다른 Spring Bean의 트랜잭션 메서드를 호출하면 기존 트랜잭션에 참여할 수 있습니다. 같은 객체 내부의 메서드를 직접 호출하는 self-invocation은 프록시를 거치지 않으므로 내부 애너테이션만 보고 새 트랜잭션 경계가 생긴다고 해석하면 안 됩니다.'),
P('PortalService의 오버로드된 updateEmployee처럼 바깥 메서드가 이미 변경 트랜잭션인 경우, 내부 호출이 별도 프록시를 거치지 않아도 바깥 트랜잭션 안에서 작업합니다. 반대로 바깥 경계가 없는데 내부 메서드의 애너테이션만 기대하는 코드를 만들면 동작이 달라질 수 있습니다.'),
N('분석','프로필 업로드는 현재 editable에서 직원 행 잠금을 먼저 얻고 이미지 디코딩·변환을 수행합니다. 변환이 오래 걸리면 잠금 보유 시간이 늘 수 있습니다. 실제 병목으로 측정된 것은 아니며, DB 변경 전 변환을 수행하고 변경 직전 권한·상태를 다시 검사하는 구조를 개선 후보로 검토할 수 있습니다.'),
Q('예외를 catch하고 성공 응답을 돌려주면 자동 롤백도 유지될까요?','예외가 어디서 잡혔는지와 트랜잭션 상태에 따라 다릅니다. 트랜잭션 내부에서 예외를 삼켜 정상 종료시키면 의도와 다른 커밋이 가능하고, 내부 호출이 rollback-only로 표시하면 나중에 예상치 못한 롤백 예외가 나기도 합니다. “catch해도 항상 전부 취소”라고 일반화하지 않습니다.') ]},
{id:'concurrency',title:'08. 중복 요청: 잠금과 제약은 서로 다른 문제를 해결합니다',goal:'check-then-act 경쟁을 재현하고 각 방어선의 효과를 분리합니다.',blocks:[
C(J+'service/PortalService.java','@Transactional public void checkIn',8,['직원 행을 먼저 잠근 뒤 같은 날짜 기록 존재 여부를 검사합니다.','조회 결과가 비어 있다는 사실은 그 순간의 관찰입니다. 동시에 실행하는 요청과 직렬화하는 장치가 필요합니다.']),
C(J+'repository/EmployeeRepository.java','@Lock(LockModeType.PESSIMISTIC_WRITE)',4,['lockById는 직원 한 행, lockByRole은 역할에 해당하는 여러 행을 잠급니다.','잠금 범위가 커질수록 직렬화되는 작업이 늘어납니다.']),
T(['방어선','책임','한계'],[['버튼 disabled','사용자의 연속 클릭 완화','다른 탭·직접 요청·재시도는 막지 못함'],['PRG','새로고침 시 재제출 가능성 완화','동시 HTTP 요청 경쟁은 별개'],['행 잠금','같은 기준 자원을 사용하는 변경을 순서화','관련 경로가 같은 잠금 규약을 지켜야 함'],['UNIQUE 제약','DB에 잘못된 중복 행 저장 방지','충돌 시 사용자 응답은 앱이 처리해야 함'],['Idempotency key','동일 요청 재전송에 같은 결과 연결','키 범위·내용 불일치·보존 정책을 설계해야 함']]),
P('예약은 직원 행이 아니라 자원 행을 잠급니다. 경쟁의 단위가 같은 회의실의 시간 점유이기 때문입니다. 직원 A와 B가 서로 다른 행을 잠그더라도 같은 회의실 예약을 동시에 검사하면 충돌을 막지 못합니다. 무엇을 잠글지는 엔티티 이름보다 보호할 불변조건이 결정합니다.'),
X('동시 출근 테스트 설계','두 요청이 모두 중복 조회를 마친 다음 저장하게 만드는 상황을 생각해 보세요.',['각 요청이 별도 트랜잭션과 연결을 사용한다.','동시에 출발하도록 barrier/latch를 사용한다.','성공 건수뿐 아니라 최종 DB 행 수와 실패 유형을 확인한다.'],'한 테스트 트랜잭션 안에서 같은 메서드를 두 번 순서대로 호출하는 것은 동시성 검증이 아닙니다. 아래 시뮬레이터에서 경합 타임라인을 먼저 확인하세요.') ]},
{id:'versions',title:'09. 낙관적 버전과 업무 스냅샷을 함께 사용하는 이유',goal:'승인 시점과 신청 시점 사이의 변경을 명확하게 다룹니다.',blocks:[
C(J+'service/AttendanceCorrectionService.java','var original=attendance.findBy',6,['신청 당시 원본 버전과 출퇴근 값을 저장합니다.','원본이 없었던 경우는 null로 표현하여 기존 행 수정과 신규 행 생성을 구분합니다.']),
C(J+'service/AttendanceCorrectionService.java','if(approve) {',10,['승인 직전에 실제 기록과 스냅샷을 대조합니다.','버전뿐 아니라 원래 시각까지 확인하여 오래된 신청의 덮어쓰기를 차단합니다.']),
P('@Version은 엔티티 UPDATE 시 경쟁을 감지하는 기술적 장치입니다. originalVersion은 “이 신청은 어떤 원본을 보고 작성했는가”를 나타내는 업무 데이터입니다. 사용자가 승인까지 기다리는 긴 시간 동안 DB 트랜잭션을 계속 열어 둘 수 없으므로 스냅샷을 저장해 나중에 비교합니다.'),
Q('원본이 바뀌면 최신 값으로 자동 병합하면 더 편하지 않을까요?','시간 정정은 신청자가 의도한 근무 기록과 승인자의 판단에 영향을 줍니다. 숫자나 시각을 임의로 병합하면 승인 대상이 달라집니다. 현재는 반려 후 재신청을 요구하는 명시적 충돌 정책을 사용합니다.'),
X('충돌 테스트 추가 설계','정정 신청 후 실제 출퇴근을 변경하고 이전 신청을 승인해 보세요.',['원본 시각이 덮이지 않는다.','신청 승인 상태와 알림이 부분 반영되지 않는다.','원본 부재→생성, 기존 원본→수정의 두 경우를 포함한다.'],'예외 메시지만 검사하지 말고 DB 상태를 다시 조회해 불변조건을 확인하세요.') ]},
{id:'states',title:'10. 승인 시스템은 상태 전이와 불변조건의 집합입니다',goal:'상태 문자열을 단순 표시가 아닌 허용 행동의 기준으로 읽습니다.',blocks:[
T(['기능','전이 예','보호 조건'],[['휴가·초과근무','PENDING → APPROVED/REJECTED/CANCELLED','대기 상태·적격 결재자·자기 결재 금지'],['전자결재','PENDING + currentStep → 다음 단계/최종 상태','현재 순번의 결재자만 처리'],['예약','PENDING → ACTIVE/REJECTED/CANCELLED','자원 활성·시간 유효·관리자 허가'],['승인 변경','PENDING 변경 요청 → 원본 취소 + 새 승인 연결','원본 버전 유지·변경 내용 유효']]),
C(J+'service/GroupwareService.java','step.setStatus(approve?',5,['현재 단계의 처리 결과와 시각을 저장합니다.','중간 승인에서는 문서가 여전히 PENDING이고 currentStep만 증가합니다.','전체 문서 승인과 개별 단계 승인을 같은 필드로 표현하지 않습니다.']),
P('휴가는 대기 중에도 차감량을 예약합니다. remaining은 저장 필드가 아니라 연간 부여 단위에서 PENDING/APPROVED 차감량을 뺀 계산 결과입니다. 상태 전이를 바꾸면 UI 라벨뿐 아니라 연차 통계·취소 가능 여부·알림·보고서까지 영향을 받습니다.'),
N('개선 실습','일부 기능은 enum, 일부는 String 상태를 사용합니다. 상태를 enum으로 바꾸는 실습은 오타 방지 장점이 있지만 기존 DB 문자열·JSON·템플릿 비교식·마이그레이션까지 점검해야 합니다. 단순 타입 교체로 완료되는 작업은 아닙니다.'),
X('상태 전이 표로 테스트 만들기','예약의 각 현재 상태에서 승인·반려·취소가 가능한 사람을 표로 만드세요.',['이미 처리한 예약의 중복 승인 거부','사용 중지 자원 승인 거부','예약 시작 이후 승인 제한','다른 직원의 취소 요청 거부'],'서비스의 require 순서도 확인하세요. 어떤 조건에서 어떤 안내가 나오는지 사용자 경험에 영향을 줍니다.') ]},
{id:'time',title:'11. 날짜·시간·금액은 도메인 타입으로 다룹니다',goal:'시간대, 경계, 단위, 정밀도에서 생기는 오류를 예방합니다.',blocks:[
C(J+'service/OvertimeService.java','var now=LocalDateTime.now(clock);',7,['오늘 날짜 허용과 오늘의 미래 시각 허용은 다른 문제입니다. date.atTime(end)까지 비교합니다.','분 단위 입력·입사일·종료 순서·이미 종료된 구간이라는 규칙을 함께 검사합니다.']),
P('Clock을 주입하면 실제 시스템 시계를 바꾸지 않고 테스트 시간을 고정할 수 있습니다. LocalDate는 날짜, LocalTime은 시각, LocalDateTime은 시간대 없는 날짜·시각입니다. 현재 앱은 Asia/Seoul 해석을 일관되게 사용하는 구조이며, 이 타입만으로 전 세계 시간대 정보를 보존하는 것은 아닙니다.'),
C(J+'service/AttendanceReportService.java','private long workedMinutes',12,['실제 근무와 점심 구간의 교집합만 제외합니다.','원래 초 단위 값을 누적한 뒤 분으로 변환하므로 반올림·절삭 위치도 결과에 영향을 줄 수 있습니다.']),
P('전자결재 금액은 BigDecimal을 사용합니다. 양수, 소수 둘째 자리, 최대 값을 검사하여 DB NUMBER(15,2) 범위와 업무 입력을 맞춥니다. 연차는 반차를 1단위로 표현한 정수 모델입니다. 같은 “숫자”라도 시간·금액·연차의 저장 단위를 별도로 정의해야 합니다.'),
X('경계값 목록 작성','09:00:00/09:00:01, 18:00 전후, 점심 경계, 윤년·월말·연말을 테스트 표로 만드세요.',['휴가와 초과근무의 미래 날짜 정책이 다름을 구분한다.','맞닿은 예약 구간을 겹침으로 오판하지 않는다.','퇴근 미기록은 완료 근무시간 0으로 처리한다.'],'Clock.fixed와 명시적 날짜·시각으로 재현 가능한 테스트를 설계하세요.') ]},
{id:'queries',title:'12. 조회 설계: findAll, fetch join, DTO, 페이징',goal:'조회 결과가 맞는 것과 조회 비용이 적절한 것을 분리해서 평가합니다.',blocks:[
C(J+'repository/ChatMessageRepository.java','@Query("select m from ChatMessage m join fetch',2,['두 참여자 조건과 before 기준이 DB WHERE에 들어갑니다.','sender·recipient fetch join으로 DTO 작성에 필요한 관계를 함께 가져옵니다.']),
C(J+'service/ChatService.java','public History history',8,['51개 조회 → 최대 50개 표시 → hasMore 판단으로 다음 페이지 존재를 확인합니다.','화면 표시는 ID 오름차순으로 다시 정렬해 오래된 메시지를 먼저 보여 줍니다.']),
P('GroupwareService에는 findAll 후 stream.filter 경로가 많습니다. 직원 관리의 10명 페이지도 서버가 전달한 목록을 JavaScript에서 나누는 방식입니다. 화면에 10명만 보인다고 DB에서 10명만 읽는 것은 아닙니다. 데이터가 늘면 권한 조건을 포함한 DB 조회와 서버 페이징으로 옮길 후보입니다.'),
P('키셋 조회는 마지막으로 본 ID 이전/이후를 기준으로 이동하므로 큰 offset을 건너뛰는 비용을 줄일 수 있습니다. 하지만 ID 생성 순서는 트랜잭션 커밋 순서와 항상 같지 않습니다. 현재 before 페이징은 이전 기록 탐색에 사용하며, 이것만으로 어떤 지연·동시성에서도 메시지 누락이 없다고 주장할 수는 없습니다.'),
N('분석','채팅 클라이언트는 최신 50개와 보유 메시지를 합칩니다. 오랫동안 연결이 끊겨 그 사이 50개보다 많은 메시지가 도착하면 중간 구간 복구를 별도로 검토해야 합니다. 정상 연결의 가벼운 대화 테스트와 장기 단절 복구 보장은 다릅니다.'),
X('내 업무 서버 페이징 설계','담당자·검색어·상태 조건을 Repository 조회로 이동하는 설계를 작성하세요.',['권한 조건을 DB 조회에서 누락하지 않는다.','total/count와 정렬 기준을 명시한다.','동일 마감일이면 ID 같은 보조 정렬 키를 둔다.','빈 검색어·기한 초과·다른 사용자 ID 조작을 검사한다.'],'Page는 전체 개수 조회 비용이 필요할 수 있습니다. 전체 건수가 필요한지에 따라 Slice 같은 결과 구조도 비교하되 실제 적용 전에 현재 사용 버전 API를 확인하세요.') ]},
{id:'schema',title:'13. 물리 FK와 논리 참조를 분리해 읽기',goal:'DB가 보장하는 규칙과 애플리케이션이 책임지는 규칙을 구별합니다.',blocks:[
C(J+'domain/StoredFile.java','@Entity',11,['ownerType + ownerId는 여러 종류의 대상을 연결하는 다형 참조입니다.','uploader는 직원 FK이지만 ownerId에는 대상별 물리 FK가 없습니다.']),
P('2026-09-24 메타데이터 기준 19개 테이블, 202개 컬럼, 물리 FK 30개입니다. FILE.OWNER_ID, AMENDMENT.REQUEST_ID/REPLACEMENT_ID, EMPLOYEE.PROFILE_FILE_ID는 같은 방식의 단순 외래키로 볼 수 없습니다. ERD의 점선은 의미상 연결을 표현하며 DB가 참조 무결성을 자동 보장한다는 뜻은 아닙니다.'),
T(['설계 선택','장점','주의점'],[['공통 파일 테이블','첨부 바이트·메타데이터 저장 재사용','owner 타입별 서비스 권한·대상 검사 필요'],['프로필 파일 ID','직원 조회에서 BLOB 분리','교체·삭제 시 고아 파일 방지 필요'],['문서와 결재 단계 분리','단계별 상태·시각·결재자 관리','currentStep과 단계 목록 일관성 필요'],['직원 부서 문자열','단순한 조직 관리','조직명 변경·이동 이력·오탈자 정책 필요']]),
Q('논리 참조는 나쁜 설계이므로 전부 FK로 바꿔야 할까요?','트레이드오프입니다. 다형 참조를 여러 연결 테이블이나 별도 파일 소유 모델로 바꾸면 DB 제약은 강해질 수 있지만 모델·조회·마이그레이션이 복잡해집니다. 실제 삭제·이관·감사 요구를 기준으로 선택해야 합니다.'),
X('고아 파일 검사 계획','파일은 존재하지만 첨부 대상이 없거나 profileFileId와 일치하지 않는 경우를 탐지하는 읽기 전용 검사를 설계하세요.',['진단과 삭제를 분리한다.','PROFILE과 일반 첨부의 보존 규칙을 구분한다.','원래 연결이 확인되지 않으면 자동 삭제하지 않는다.'],'처음에는 대상 타입별 매칭 수와 불일치 목록만 만들고 실제 정리는 별도 검토합니다.') ]},
{id:'idempotency',title:'14. 채팅 전송을 멱등하게 만드는 설계',goal:'응답 유실을 실패 저장으로 오해하지 않고 안전하게 재시도합니다.',blocks:[
C(J+'service/ChatService.java','@Transactional public Message send',17,['발신자 행 잠금으로 같은 발신자의 동시 전송·재시도 처리를 순서화합니다.','이미 있는 clientId이면 상대와 내용까지 비교하고 기존 Message를 반환합니다.','키가 같지만 요청 내용이 다르면 기존 결과를 조용히 재사용하지 않고 거부합니다.']),
C(J+'domain/ChatMessage.java','@Table',8,['UK(sender_id, client_id)가 DB의 마지막 중복 방지선입니다.','키는 발신자 범위이므로 서로 다른 직원이 같은 UUID 문자열을 사용해도 별도 요청입니다.']),
P('클라이언트는 글 내용이 같고 실패 후 다시 보내는 경우 retries에 있던 clientId를 유지합니다. 서버가 저장했지만 응답만 유실됐을 수 있기 때문입니다. 재시도마다 UUID를 새로 만들면 서버에는 다른 메시지로 보입니다. 반대로 서로 다른 글에 같은 키를 쓰면 의도를 구분할 수 없으므로 서버가 충돌을 알려야 합니다.'),
N('현재 구현의 범위','재시도 키와 작성 중 글은 브라우저 메모리에 보관합니다. 페이지 새로고침·브라우저 종료를 넘는 전송 복구까지 제공하는 것은 아닙니다. 이런 보장이 필요하다면 로컬 저장소 보존·정리·공용 PC 노출 정책을 함께 설계해야 합니다.'),
X('응답 유실 테스트','서버까지 요청이 도달해 저장됐지만 클라이언트에는 네트워크 오류처럼 보이게 만드세요.',['같은 키 재전송 후 DB 메시지는 1건','같은 키 + 다른 내용은 거부','같은 본문 + 다른 키는 사용자가 의도한 별도 전송일 수 있음','다른 직원의 senderId를 body로 보내도 발신자는 인증 사용자'],'기존 check-chat.cjs는 별도 테스트 DB에서 응답 유실을 흉내 냈습니다. 실제 동료 계정에 메시지를 보내며 검증하지 않습니다.') ]},
{id:'async',title:'15. 비동기 UI는 요청 순서와 응답 순서가 다릅니다',goal:'상태 머신·세대 번호·버튼 잠금·메모리 수명을 분석합니다.',blocks:[
C(R+'static/js/chat.js','async function history',12,['요청을 시작한 revision과 응답 시점 revision을 비교합니다.','대화 A를 읽다가 B로 이동한 뒤 A 응답이 늦게 와도 B 화면을 덮지 않게 합니다.','messages Map은 ID 기준 병합이므로 동일 항목 재조회가 DOM 중복으로 연결되지 않도록 돕습니다.']),
P('mode는 list/people/thread, peer는 현재 상대, revision은 화면 전환 세대, sending·reading·polling은 작업 진행 상태입니다. 단순 boolean 하나로 모든 상태를 처리하면 조회 중 상대 변경과 전송 중 화면 닫기 등 조합이 꼬일 수 있습니다. 현재 코드는 변수 조합으로 상태를 표현하므로 가능한 조합을 표로 적어 검토하면 좋습니다.'),
C(R+'static/js/chat.js','async function poll()',4,['polling 상태로 동시에 여러 조회가 실행되는 것을 제한합니다.','숨겨진 탭에서는 요청을 쉬고, 다음 확인 타이머를 예약합니다.','finally에서 다음 조회를 예약하므로 정상·실패 양쪽에서 반복 흐름이 이어집니다.']),
P('AbortController로 이전 요청을 취소하는 방식도 가능하지만, 취소와 최신성 검사는 같은 문제가 아닙니다. 응답이 이미 도착했거나 취소가 늦게 처리될 수 있으므로 현재 화면에 적용 가능한 결과인지 확인하는 정책은 별도로 필요합니다. 취소는 개선 실습 후보이고 현재 구현의 필수 구성이라고 설명하지 않습니다.'),
Q('sending=true이면 모든 중복 메시지를 막을 수 있을까요?','아닙니다. 같은 화면의 중복 클릭을 줄일 뿐 새 탭·새로고침·네트워크 재전송·서버 응답 유실은 별개입니다. 클라이언트 상태 제어와 서버 멱등성이 함께 필요합니다.'),
X('응답 역전 시나리오','A 대화의 응답을 지연시키고 B 대화로 옮긴 다음 A 응답을 도착시키세요.',['B 제목과 메시지 목록이 유지된다.','A의 오류가 B 화면에 잘못 표시되지 않는다.','닫힘·재열림·검색 전환도 포함한다.'],'Mock API나 브라우저 route 지연으로 재현하고 실제 운영 메시지를 변경하지 않는 환경을 사용하세요.') ]},
{id:'readreceipts',title:'16. 읽음 처리의 기준을 명시적으로 정의하기',goal:'조회·노출·인지가 다르다는 점을 API와 UI에 반영합니다.',blocks:[
C(R+'static/js/chat.js','async function readVisible()',7,['패널 열림·문서 가시성·포커스·다른 dialog 상태를 확인합니다.','보이는 영역과 메시지 bounding box가 겹치는 수신 메시지 ID만 요청합니다.']),
C(J+'repository/ChatMessageRepository.java','@Modifying',3,['서버는 recipient=나, sender=상대, readAt is null, 요청 IDs 조건을 동시에 적용합니다.','프런트가 보낸 IDs 전체를 무조건 읽음으로 바꾸지 않습니다. bulk update이므로 version도 직접 증가시킵니다.']),
P('읽음은 사용자가 의미를 이해했다는 보장이 아닙니다. 현재 제품의 기준은 “읽음 처리 조건을 만족하는 화면에 메시지가 노출되었다”입니다. GET history에 부수효과로 읽음을 넣으면 백그라운드 프리로드·닫힌 창 조회도 읽음이 될 수 있어 명시적 POST로 분리했습니다.'),
Q('bulk update가 실행되면 이미 로딩한 모든 엔티티 값도 즉시 바뀌나요?','JPQL bulk update는 영속성 컨텍스트의 관리 객체를 거쳐 setter를 호출하는 방식이 아닙니다. 같은 컨텍스트에 이전 상태의 객체가 있으면 불일치가 생길 수 있어 조회·clear·트랜잭션 경계를 고려해야 합니다. 현재 읽음 요청 경로에서 무엇을 미리 로딩하는지 확인하세요.'),
X('읽음 경계 테스트','다른 상대 메시지 ID와 내가 발신한 메시지 ID를 읽음 요청에 섞어 보내 보세요.',['내가 수신한 현재 상대 메시지만 변경된다.','같은 읽음 요청을 반복해도 최초 readAt을 덮지 않는다.','history GET만 실행하면 readAt이 null로 남는다.'],'ChatTests의 명시적 읽음 범위 테스트를 출발점으로 사용하세요.') ]},
{id:'upload',title:'17. 업로드는 형식·자원·권한의 세 경계를 통과합니다',goal:'확장자·MIME·파일 크기 검사만으로 끝나지 않는 이유를 분석합니다.',blocks:[
C(J+'service/ProfileImageService.java','private byte[] normalize',17,['업로드 바이트 제한과 해상도 제한을 분리합니다. 압축 파일 크기가 작아도 디코딩 시 메모리를 크게 사용할 수 있습니다.','ImageReader의 실제 형식을 확인하고 허용한 JPEG/PNG만 읽습니다.','중앙 정사각형을 256×256 PNG로 재인코딩합니다. 원본의 메타데이터를 복사하지 않습니다.']),
C(J+'service/ProfileImageService.java','@Transactional public void upload',9,['정규화 결과를 portal_file PROFILE 유형으로 저장한 뒤 직원의 현재 파일 ID를 바꿉니다.','이전 파일 삭제까지 같은 변경 트랜잭션에 포함합니다.']),
P('클라이언트 accept는 선택 UI 힌트이지 보안 검증이 아닙니다. 일반 첨부와 프로필의 제약도 다릅니다. 일반 파일 기본 제한은 10MB, 프로필 서비스는 5MB·1,600만 픽셀·허용 이미지 형식을 추가로 검사합니다. 출력은 image/png, nosniff, 로그인 필요, no-store 정책으로 제공됩니다.'),
N('분석','현재 변환은 EXIF 회전 값을 별도로 반영하는 코드를 두지 않습니다. 휴대전화 사진 방향까지 요구사항에 들어간다면 회전 메타데이터 해석과 재인코딩 결과를 별도 테스트해야 합니다. 모든 이미지 방향이 보정된다고 주장하지 않습니다.'),
X('기존 사진 보존 검증','정상 사진 업로드 후 가짜 PNG와 과대 파일을 올려 보세요.',['실패 요청 뒤 profileFileId와 이전 BLOB이 유지된다.','다른 직원은 수정할 수 없다.','관리자 교체 뒤 이전 파일만 삭제된다.','이미지 로딩 실패 시 첫 글자 대체 표시가 남는다.'],'ProfileImageTests에서 DB 상태 검증과 브라우저 이미지 표시 검증을 분리해 확장하세요.') ]},
{id:'viewmodels',title:'18. SSR 화면 모델과 UI 상태를 분리하기',goal:'서버 데이터, HTML 표현, 브라우저 상태 각각의 책임을 구분합니다.',blocks:[
C(J+'web/PortalController.java','if(admin) {',13,['서버가 검색·부서·역할·상태 필터를 적용한 목록과 선택 옵션을 Model에 담습니다.','JavaScript 10명 단위 페이지는 이 목록을 다시 나눠 보여 주는 표현 계층의 상태입니다.']),
P('Thymeleaf의 th:text는 서버 데이터를 이스케이프하여 텍스트로 삽입합니다. 클라이언트 채팅도 textContent를 사용합니다. HTML 문자열 합성으로 렌더링을 바꾸려면 사용자 입력이 태그·이벤트 속성으로 실행되지 않도록 escaping 경계를 다시 설계해야 합니다.'),
C(R+'static/js/employees.js','document.querySelectorAll(\'[data-edit-employee]\')',9,['data-* 속성에서 현재 수정 대상 값을 읽어 폼 action과 입력을 채웁니다.','직원 정보 폼과 프로필 사진 폼은 별도 요청이며 저장되는 시점도 다릅니다.']),
P('dialog의 showModal은 키보드 포커스와 모달 동작을 제공합니다. 접근성은 aria 속성을 붙이는 것만으로 끝나지 않습니다. 열릴 때 초점, Escape 닫기, 닫힌 뒤 초점 복귀, 오류가 있는 입력 탭 노출, 모바일에서 버튼이 화면 안에 있는지까지 확인합니다.'),
X('직원 편집 UX 검증','직원 A의 패널을 닫고 B의 패널을 연 뒤 모든 값과 사진 대상이 B로 바뀌는지 확인하세요.',['file input의 이전 선택이 초기화된다.','삭제 확인의 대상 ID·로그인 ID가 B와 일치한다.','키보드만으로 열기·수정·닫기를 할 수 있다.'],'DOM에서 버튼을 숨겼는지보다 서버가 현재 수정 대상에 대한 권한을 확인하는지도 함께 테스트합니다.') ]},
{id:'testing',title:'19. 테스트를 계층별로 나누고 주장 범위를 제한하기',goal:'서비스 규칙·HTTP 보안·실제 브라우저·DB 차이를 각각 검증합니다.',blocks:[
T(['검증 층','적합한 질문','현재 도구'],[['서비스 규칙','상태 전이·차감·소유권이 맞는가','Spring Boot Test + JUnit'],['HTTP 계약','인증·CSRF·응답 상태·JSON이 맞는가','MockMvc + Security test'],['브라우저','폼·포커스·모바일·비동기 상태가 맞는가','Playwright + Edge'],['실제 DB','Oracle 타입·SQL·잠금이 맞는가','스키마 검증 + 제한된 실제 실행 확인']]),
C('src/test/java/org/example/portal/ChatTests.java','@Test void retriesAreIdempotent',11,['같은 키 재전송의 ID가 같다는 것을 확인합니다.','body에 senderId를 추가해도 인증된 발신자로 처리되는 HTTP 경계를 확인합니다.']),
P('현재 실행 기록의 66개 자동 테스트는 H2 환경에서 통과했습니다. Oracle 11g의 전체 자동 통합 테스트나 부하 테스트 통과로 확장해서 설명하면 안 됩니다. H2의 Oracle 호환 모드는 SQL·잠금·LOB·드라이버의 모든 차이를 재현하지 않습니다.'),
P('롤백되는 테스트 트랜잭션은 실제 요청의 커밋·연결·동시 실행과 다른 조건을 만들 수 있습니다. 변경 감지 확인에는 편하지만 두 요청의 경쟁을 검증하려면 각 요청의 트랜잭션이 분리되어야 합니다. 테스트 순서에 의존하지 않도록 고유 계정과 독립 데이터도 필요합니다.'),
X('실패 후 상태를 확인하는 테스트','승인 변경 replace 과정에서 검증 오류가 발생하는 경우를 만드세요.',['응답 오류만 보지 말고 원본 상태를 새로 읽는다.','신청 상태·후속 신청·알림이 부분 저장되지 않았는지 본다.','성공 경로와 실패 경로를 다른 테스트로 분리한다.'],'예외 메시지 하나와 구현 내부 메서드 호출 횟수만 검사하면 업무 결과의 회귀를 놓칠 수 있습니다.') ]},
{id:'operations',title:'20. 실행 프로필·이미지·배포 환경을 혼동하지 않기',goal:'설정 우선순위와 배포 경계를 이해하고 재현 가능한 실행을 준비합니다.',blocks:[
C('Dockerfile','FROM maven',13,['빌드 단계에서는 Maven을 사용하고 실행 단계에는 JRE와 JAR를 넣습니다.','Docker 빌드의 -DskipTests를 테스트 통과 증거로 사용하지 않습니다.']),
C('compose.yaml','environment:',12,['Oracle 연결 URL을 컨테이너에서 PC에 접근 가능한 주소로 지정합니다.','민감한 설정 파일은 이미지에 넣지 않고 읽기 전용 마운트로 제공합니다.']),
P('소스를 바꿔도 이미 실행 중인 JAR는 바뀌지 않습니다. 이미지 재빌드와 컨테이너 재생성이 필요합니다. 환경변수·마운트 같은 실행 설정 변경은 이미지 빌드와 별개이지만 컨테이너에 새 설정을 적용해야 합니다. 문서만 바꾸는 작업은 앱 리빌드가 필요하지 않습니다.'),
P('AWS 테스트는 EC2의 Caddy + Spring Boot와 PC Oracle을 역방향 SSH 터널로 연결한 구성입니다. RDS/S3 기반 구성은 아니며 PC·Oracle·터널에 의존합니다. 테스트는 종료됐고 이후 기능은 로컬에 먼저 반영했습니다. 여기서 상시 운영으로 전환하려면 DB 위치·백업·모니터링·비밀값 관리·복구를 따로 설계해야 합니다.'),
X('배포 점검 문서 작성','팀원이 다른 PC에서 실행할 수 있도록 필요한 파일과 값을 분류하세요.',['소스·이미지·설정·DB 구조·DB 데이터·인증서를 별개로 구분한다.','초기 생성 SQL과 기존 DB 마이그레이션을 혼동하지 않는다.','시작 확인과 종료·재시작·문제 시 복구 절차를 적는다.'],'docker compose down이 PC Oracle 데이터를 지우는 동작은 아니지만, 별도 환경에서 볼륨 삭제 옵션을 사용하면 영향이 달라질 수 있습니다.') ]},
{id:'refactoring',title:'21. 현재 구조에서 시작하는 리팩터링 로드맵',goal:'측정·요구사항·회귀 테스트를 근거로 개선 순서를 정합니다.',blocks:[
N('개선 실습','아래 항목은 제안입니다. 앱에 반영됐거나 성능 개선이 측정됐다는 의미가 아닙니다. 기존 사용자 흐름과 데이터 의미를 유지하는 테스트를 먼저 준비합니다.'),
T(['후보','필요해지는 이유','먼저 검증할 것'],[['GroupwareService 분리','서로 다른 변경 이유가 한 클래스에 모임','서비스 경계를 바꿔도 트랜잭션·권한 유지'],['서버 페이징·조건 쿼리','findAll·Java 필터의 데이터량 증가','현재 정렬·공개 범위·검색 의미 유지'],['채팅 단절 복구','최신 50개 밖의 미수신 구간','장기 단절·ID와 커밋 순서·중복 병합'],['이미지 변환과 잠금 분리','CPU 작업 동안 DB 잠금 유지','변환 후 저장 직전 권한·상태 재검사'],['정기 알림 확장','앱 인스턴스가 여러 개가 됨','업무 행 잠금·재확인·알림 중복·실패 재시도'],['조직 이력 모델','부서 이동 전 상태의 보고서 요구','현재 소속 기준과 과거 시점 기준 구분']]),
C(J+'config/TaskReminders.java','@Scheduled',1,['fixedDelay는 이전 작업 종료 후 다음 실행까지의 지연입니다. 앱 인스턴스마다 스케줄러가 실행될 수 있다는 운영 조건을 고려해야 합니다.']),
P('현재 remindTasks는 업무별 잠금 뒤 reminderDate를 다시 검사합니다. 따라서 스케줄러가 두 개면 무조건 중복 알림이라고 단정해서는 안 됩니다. 그렇다고 분산 운영 전체가 검증됐다는 뜻도 아닙니다. 잠금 경합, 부분 실패, 알림 외부 전송 도입, 작업량 증가를 구체 시나리오로 나누어 평가합니다.'),
X('작은 PR 단위로 설계하기','하나의 후보를 선택해 문제·현재 동작·변경 후 동작·검증·롤백을 한 페이지로 작성하세요.',['기술 이름 도입 자체를 목표로 삼지 않는다.','기능 개선과 정리 작업을 구분한다.','성능 주장은 전후 측정 항목과 데이터를 정한다.'],'예: “업무 목록의 서버 페이지 크기를 20으로 제한하고 내 업무 조건을 쿼리에 포함한다”는 “JPA 최적화”보다 검증하기 쉽습니다.') ]},
{id:'practice',title:'22. 4주 학습 계획과 최종 설계 리뷰',goal:'읽기에서 재현·검증·설명으로 학습을 마무리합니다.',blocks:[
T(['주차','실습 주제','완료 산출물'],[['1주','MVC·보안·요청 계약','출근과 채팅 요청 흐름도 + 권한 표'],['2주','트랜잭션·상태·동시성','경계값·중복·충돌 테스트 계획과 실행 기록'],['3주','조회·비동기 UI·업로드','조회 비용 관찰 + 응답 역전·재시도 테스트'],['4주','리팩터링·배포·발표','작은 개선안 + 회귀 근거 + 운영 절차 문서']]),
L('출근 버튼을 눌렀을 때 인증 정보가 어떤 경로로 Service에 도달하는가?','변경 감지는 어디서 일어나고 flush와 commit은 어떻게 다른가?','같은 자원 예약을 서로 다른 직원이 동시에 시도하면 무엇을 잠가야 하는가?','정정 신청 스냅샷과 @Version은 각각 어떤 시간을 보호하는가?','clientId가 같아도 다른 발신자가 사용하면 어떤 결과가 나오는가?','채팅 응답이 역순으로 도착했을 때 현재 화면을 어떻게 보호하는가?','화면 페이징과 DB 페이징의 비용 차이를 어떻게 측정할 것인가?','관리자 권한으로도 볼 수 없는 데이터는 무엇이고 어디서 막는가?','H2 통합 테스트 결과로 Oracle 운영 안정성을 어디까지 주장할 수 있는가?','AWS의 웹 앱이 켜져 있어도 PC Oracle이 꺼지면 왜 실패하는가?'),
P('각 답은 “일반적으로 그렇다”에서 끝내지 말고 파일명·메서드·조건·테스트로 연결하세요. 현재 구현의 한계를 말할 수 있는 것도 이해의 일부입니다. 개선안을 현재 기능인 것처럼 섞지 않고, 변경하면 어떤 불변조건을 다시 검증해야 하는지 설명하면 설계 리뷰와 발표 모두에 도움이 됩니다.'),
Q('완료 기준','세 기능의 요청 흐름을 실제 코드로 추적하고, 한 가지 실패·동시성 상황을 재현하고, 하나의 개선안을 회귀 테스트와 함께 설명할 수 있다면 이 학습서의 목표에 도달한 것입니다.')]}
];
