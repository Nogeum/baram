// Authored lessons for the current source snapshot, not a reconstruction of commit history.
const P=t=>({kind:'p',text:t}),H=t=>({kind:'h',text:t}),L=(...items)=>({kind:'list',items}),T=(headers,rows)=>({kind:'table',headers,rows}),C=(file,needle,count,explanations=[])=>({kind:'code',file,needle,count,explanations}),Q=(question,answer)=>({kind:'quiz',question,answer}),N=t=>({kind:'note',text:t}),F=(...items)=>({kind:'flow',items});
const J='src/main/java/org/example/portal/',R='src/main/resources/';
module.exports=[
{id:'start',title:'00. 이 책을 읽는 방법',tag:'처음 만나는 프로젝트',blocks:[
P('이 프로젝트의 이름은 “바람의 나라”입니다. 게임이 아니라, 회사 사람들이 출근을 기록하고 휴가를 신청하며 서로 이야기하는 사내 업무 포털입니다. 학교로 비유하면 출석부, 체험학습 신청서, 선생님 결재함, 공지 게시판, 친구에게 보내는 쪽지함을 하나의 웹사이트에 모은 셈입니다.'),
P('여기서 “만들었다”는 말은 예쁜 화면만 그렸다는 뜻이 아닙니다. 누가 버튼을 눌렀는지 확인하고, 그 사람이 할 수 있는 일인지 판단하고, 내용을 오래 보관하고, 다음에 다시 보여주는 일까지 연결했다는 뜻입니다.'),
N('현재 소스의 동작을 이해하기 좋은 순서로 재구성한 학습서입니다. 실제 첫날부터 마지막 날까지의 개발 기록이나 팀원별 기여를 재현한 문서는 아닙니다. README에는 초기 설명이 남아 있어, 설명이 다르면 현재 코드와 실제 DB 구조를 우선합니다.'),
L('처음에는 01~10장을 순서대로 읽으세요. Java와 웹의 기본 약속을 배웁니다.','그다음 출근 버튼의 여행을 따라가세요. 이후 기능도 거의 같은 길을 걷습니다.','각 코드 상자의 왼쪽은 실제 원문, 오른쪽은 그 줄을 읽는 설명입니다. 긴 줄은 가로로 움직여 볼 수 있습니다.','마지막 “전체 코드 사전”에서는 실행 코드·화면·설정·DB SQL·테스트 파일을 하나씩 고를 수 있습니다.','기억이 안 나는 단어는 용어 사전을, 잘 이해했는지는 접을 수 있는 질문의 답을 확인하세요.'),
P('실제 비밀번호가 들어 있는 config 폴더, 개인 계정 목록, 다운로드한 라이브러리 수만 줄, 빌드 결과는 책에 넣지 않았습니다. 읽을 대상은 우리가 작성한 실행 코드와 그 코드를 이해하는 데 필요한 설정·테스트입니다. 참고용 원본 화면(example)은 실제 사용 화면과 구분합니다.'),
Q('웹사이트가 화면 파일만 있으면 완성될까요?','아니요. 화면은 입력을 받는 창문입니다. 누가 요청했는지 확인하는 문지기, 업무 규칙을 판단하는 담당자, 기록을 보관하는 DB가 함께 필요합니다.')]},
{id:'web',title:'01. 웹사이트는 요청과 응답으로 대화합니다',tag:'인터넷 기초',blocks:[
P('브라우저는 크롬이나 엣지처럼 웹페이지를 보여주는 프로그램입니다. 서버는 브라우저의 부탁을 받아 처리하는 프로그램입니다. 이 프로젝트에서는 Java로 만든 Spring Boot 프로그램이 서버 역할을 합니다. Oracle은 서버가 기록을 보관할 때 찾아가는 데이터베이스입니다.'),
F('브라우저: 출근할게요','서버: 로그인·중복 확인','DB: 출근 기록 저장','서버: 결과 화면 응답'),
P('요청에는 주소와 행동 종류가 있습니다. GET은 “보여 주세요”, POST는 “이 내용을 처리해 주세요”에 가깝습니다. GET /employee/main은 대시보드를 달라는 부탁이고, POST /employee/check-in은 출근을 처리해 달라는 부탁입니다. 실제 자료를 바꾸는 요청을 주소창에 글자를 치는 것만으로 실행하도록 만들지 않습니다.'),
T(['표현','뜻','프로젝트 예'],[['URL','요청할 곳의 주소','/employee/main'],['HTTP','브라우저와 서버 사이 대화 규칙','GET /login'],['200','요청 처리 성공','로그인 화면 표시'],['302 계열 이동 응답','다른 주소로 다시 가라는 안내','처리 후 대시보드로 이동'],['400','입력이나 업무 조건이 잘못됨','겹치는 휴가 신청'],['403','이 일을 할 권한이 없음','직원이 관리자 주소에 접근'],['404','해당 대상이나 주소가 없음','없는 사진 요청']]),
P('localhost는 “지금 이 컴퓨터”라는 별명입니다. 8080은 건물의 방 번호처럼 서버가 요청을 듣는 포트입니다. localhost:8080에 들어간다는 것은 이 컴퓨터의 8080번 문을 두드린다는 뜻입니다. 다른 컴퓨터에서 localhost를 입력하면 그 다른 컴퓨터를 가리킵니다.'),Q('휴가 신청 화면을 보는 것과 휴가 신청을 제출하는 것은 왜 다른 요청일까요?','보는 것은 조회이고, 제출하는 것은 새 기록을 만드는 행동이기 때문입니다. 같은 기능 안에서도 GET과 POST의 책임을 나눕니다.')]},
{id:'map',title:'02. 폴더는 담당자별 서랍입니다',tag:'프로젝트 지도',blocks:[
P('코드를 한 파일에 전부 넣으면 길을 잃기 쉽습니다. 그래서 역할별로 서랍을 나눕니다. 학교에 교무실, 행정실, 도서관이 나뉘어 있는 것과 비슷합니다. 파일 이름을 외우기 전에 어느 서랍의 일인지 이해해 보세요.'),
T(['폴더·파일','쉬운 비유','여기서 하는 일'],[['pom.xml','준비물 목록','Java 버전, Spring, DB 드라이버, 빌드 도구'],['domain','기록 양식','직원·휴가·채팅 메시지의 필드'],['repository','기록 찾기 담당','조건에 맞는 DB 자료 조회·저장·잠금'],['service','업무 판단 담당','휴가 가능 여부, 승인 권한, 시간 겹침 검사'],['web','접수 창구','주소의 요청을 받아 서비스에 전달'],['config','공통 규칙','로그인, 시계, 초기 계정, 정기 작업'],['templates','화면 설계도','서버가 값을 넣어 HTML을 완성'],['static','화면 도구','CSS·JavaScript·아이콘'],['src/test','연습 시험지','허용할 행동과 막을 행동 확인'],['deploy/aws','외부 배포 준비','EC2와 프록시·DB 연결 구성']]),
P('가장 중요한 흐름은 Controller → Service → Repository → DB입니다. 다만 이 프로젝트는 일부 조회를 Controller가 Repository에 바로 부탁하기도 합니다. 모든 코드가 반드시 세 층을 한 번씩 지나간다고 외우면 실제 코드와 달라집니다. 중요한 것은 역할과 위험한 변경 규칙이 어디에 모였는지입니다.'),
Q('휴가를 신청할 수 있는 날짜를 바꾸고 싶다면 CSS부터 찾을까요?','아니요. CSS는 모양 담당입니다. 서버의 PortalService.requestLeave와 화면의 날짜 입력 조건을 함께 찾아야 합니다.')]},
{id:'java',title:'03. Java 문장을 읽는 기본 약속',tag:'문법 첫걸음',blocks:[
P('변수는 이름표가 붙은 상자입니다. String은 글자, int와 long은 정수, boolean은 참·거짓, LocalDate는 날짜를 담는 종류입니다. 종류를 타입이라고 부릅니다. 타입이 있으면 날짜 자리에 직원 이름을 잘못 넣는 실수를 줄일 수 있습니다.'),
T(['코드','소리 내어 읽기'],[['String name = "민수";','이름이라는 글자 상자에 민수를 넣는다'],['boolean active = true;','활성 상태를 참으로 정한다'],['if (active) { ... }','활성이 참일 때만 중괄호 안을 실행한다'],['a && b','a와 b가 모두 참이어야 한다'],['a || b','a와 b 중 적어도 하나가 참이면 된다'],['!active','활성이 아니다'],['a == null','아직 가리키는 대상이 없는가'],['name.equals(other)','두 글자의 내용이 같은가'],['조건 ? 값1 : 값2','참이면 값1, 아니면 값2를 선택한다']]),
P('세미콜론 ;은 한 명령이 끝났다는 표시입니다. 중괄호 { }는 함께 묶이는 작업의 울타리입니다. 메서드는 이름을 붙인 작업 묶음입니다. checkIn(login)은 로그인 ID를 받아 출근 작업을 실행한다는 뜻입니다. 괄호 안은 재료, return 뒤는 돌려주는 결과입니다. void는 돌려줄 결과 값이 없다는 뜻이지 아무 일도 하지 않는다는 뜻이 아닙니다.'),
P('class는 설계도이고 new는 설계도를 보고 실제 물건을 하나 만드는 말입니다. Employee 설계도는 직원의 공통 모양이고 new Employee()는 새 직원 한 명을 담을 객체입니다. DB의 직원 행과 Java 객체는 관련은 있지만 같은 물건은 아닙니다. JPA가 두 세계를 연결해 줍니다.'),
C(J+'domain/Employee.java','private LocalDate hireDate;',3,['입사일을 LocalDate 타입으로 기억합니다. private이므로 바깥에서 필드에 직접 손을 대지 않고 메서드를 이용합니다.','프로필 사진 파일의 ID를 기억합니다. Long은 null도 담을 수 있어서 “사진 없음”을 표현할 수 있습니다.','사진 ID가 null이면 URL도 없습니다. 있으면 직원 ID와 사진 파일 ID를 붙여 이미지 요청 주소를 만듭니다. 물음표 뒤 v는 URL의 조회 매개변수입니다.']),
Q('long과 Long은 왜 다르게 쓸까요?','long은 기본 정수 타입이고 Long은 정수를 감싼 객체 타입입니다. Long은 null을 이용해 아직 값이 없다는 상황도 표현할 수 있습니다.')]},
{id:'collections',title:'04. 목록에서 필요한 것만 고르는 법',tag:'Java 한 단계 더',blocks:[
P('List는 순서가 있는 목록입니다. Set은 같은 것을 중복해서 담지 않는 꾸러미입니다. Map은 “열쇠 → 값”으로 찾는 사물함입니다. 채팅에서는 메시지 ID를 열쇠로 사용하면 같은 메시지를 다시 받아도 하나로 합칠 수 있습니다.'),
P('stream()은 목록을 한 개씩 검사하는 작업 줄이라고 생각해 보세요. filter는 조건에 맞는 것만 통과시키고, map은 모양을 바꾸고, sorted는 순서를 정하고, toList는 결과를 다시 목록으로 모읍니다. Java의 Stream은 인터넷 영상 스트리밍과 다른 개념입니다.'),
C(J+'service/PortalService.java','private int reserved',5,['한 직원이 특정 연도에 예약한 연차 단위를 계산하는 작은 작업입니다. private은 이 클래스 안에서 쓰는 도우미라는 뜻입니다.','해당 직원·연도의 휴가를 찾은 후 목록을 하나씩 처리할 준비를 합니다.','PENDING 또는 APPROVED만 남깁니다. 취소나 반려는 연차를 차감하지 않습니다.','각 신청에서 chargedUnits 숫자만 꺼내 전부 더합니다. 0.5일을 1단위로 세기 때문에 정수 합계입니다.','작업 묶음이 끝납니다.']),
P('Optional은 “찾았을 수도 있고 없을 수도 있는 상자”입니다. findById(...).orElseThrow(...)는 있으면 꺼내고, 없으면 안내 오류를 낸다는 뜻입니다. ->는 “이 재료를 받아 이 일을 해라”라는 짧은 함수 표현입니다. e -> e.isActive()는 직원 e를 받아 활성 여부를 돌려줍니다. Employee::isActive도 이 상황에서는 같은 일을 가리키는 짧은 표현입니다.'),
Q('취소된 휴가까지 연차 합계에 넣으면 어떤 문제가 생길까요?','사용하지 않을 휴가인데 잔여 연차가 계속 줄어듭니다. 그래서 상태를 먼저 걸러내고 더합니다.')]},
{id:'pom',title:'05. pom.xml로 준비물을 모읍니다',tag:'프로젝트 시작',blocks:[
P('Maven은 Java 프로젝트의 준비물을 내려받고, 코드를 검사하고, 실행 파일로 포장하는 도구입니다. pom.xml은 Maven에게 주는 준비물 목록입니다. Spring Boot는 웹서버와 여러 기본 설정을 쉽게 준비하도록 돕는 출발 꾸러미입니다.'),
C('pom.xml','<parent>',6),
P('parent는 공통 설정을 물려받을 부모입니다. 이 프로젝트는 Spring Boot 4.1.1의 기본 버전 조합을 물려받습니다. java.version은 17입니다. dependency는 다른 사람이 만든 도구를 사용하겠다는 표시입니다. 모든 도구 코드를 직접 작성하는 대신 로그인, DB 연결 같은 검증된 기능을 가져옵니다.'),
T(['준비물','담당'],[['starter-webmvc','HTTP 요청을 받고 주소별 메서드 연결'],['starter-thymeleaf','화면 설계도에 데이터를 넣어 HTML 생성'],['starter-security','로그인·세션·권한·CSRF'],['starter-data-jpa','Java 객체와 DB 연결'],['ojdbc8','Oracle과 대화하는 JDBC 드라이버'],['hibernate-community-dialects','현재 Oracle 11g 환경에 맞는 SQL 방언'],['h2','개발·자동 테스트에서 쓰는 가벼운 DB'],['lombok','반복되는 getter·setter·생성자 코드 생성'],['테스트 관련 의존성','가짜 요청과 자동 확인 도구']]),
P('compile은 사람이 쓴 Java를 JVM이 실행할 수 있는 형태로 바꾸는 일입니다. test는 확인 문제를 실행하는 일, package는 JAR 실행 파일로 묶는 일입니다. .mvn과 mvnw는 Maven 실행 버전을 일정하게 맞추기 위한 래퍼입니다. 프로젝트의 실제 의존성은 pom.xml 원문에서 확인할 수 있습니다.'),Q('Maven과 Spring Boot는 같은 도구인가요?','아니요. Maven은 준비물·빌드 담당이고 Spring Boot는 애플리케이션 실행과 기본 설정을 편리하게 만드는 틀입니다.')]},
{id:'boot',title:'06. 프로그램의 첫 줄에서 서버가 켜집니다',tag:'실행 버튼',blocks:[
C(J+'PortalApplication.java','package ',13,['이 클래스가 org.example.portal이라는 코드 주소에 속한다고 선언합니다. 파일을 찾는 분류표입니다.','빈 줄은 읽기 편하게 구분한 것입니다.','SpringApplication이라는 서버 시작 도구를 가져옵니다.','SpringBootApplication 표시를 사용할 준비를 합니다.','빈 줄입니다.','이 클래스를 Spring Boot 앱의 중심으로 삼습니다. 기본 설정과 하위 패키지의 부품 찾기를 시작합니다.','PortalApplication 설계도의 시작입니다.','빈 줄입니다.','Java 실행의 출발점 main입니다. static이라 객체를 먼저 만들지 않고 호출할 수 있습니다. args에는 실행 시 전달된 문자열이 들어옵니다.','Spring에게 이 클래스를 기준으로 서버와 필요한 부품들을 준비하라고 부탁합니다.','main이 끝나는 중괄호입니다. 서버의 실행은 Spring이 관리합니다.','빈 줄입니다.','클래스 울타리가 끝납니다.']),
P('@로 시작하는 것은 애너테이션입니다. 프로그램에게 붙이는 안내 스티커와 비슷합니다. @Service는 “업무 담당 부품”, @Controller는 “요청 접수 부품”이라는 표시입니다. Spring은 이런 부품을 만들고 연결합니다. Spring이 관리하는 객체를 Bean이라고 부릅니다.'),
P('private final EmployeeRepository employees;는 이 담당자가 EmployeeRepository 도구를 계속 사용한다는 뜻입니다. @RequiredArgsConstructor가 final 필드를 받는 생성자를 만들어 줍니다. Spring이 생성자를 보고 필요한 도구를 넣어 줍니다. 이것이 의존성 주입입니다. 매번 new를 직접 쓰지 않아도 담당자에게 맞는 도구가 배달되는 셈입니다.'),
Q('Service에 Repository가 필요한데 직접 new를 하지 않은 이유는 무엇일까요?','Spring이 관리하는 Repository 구현을 생성자에 넣어 주기 때문입니다. Repository는 인터페이스인 경우가 많고 실제 구현은 Spring Data가 만들어 줍니다.')]},
{id:'settings',title:'07. 설정은 장소에 따라 옷을 갈아입습니다',tag:'프로필과 환경변수',blocks:[
C(R+'application.properties','spring.application.name',12),
P('같은 프로그램도 연습할 때와 실제 DB에 연결할 때 필요한 설정이 다릅니다. Spring profile은 상황별 설정 묶음입니다. local은 H2 개발 환경, oracle은 Oracle 환경, test는 자동 테스트 환경입니다. compose.yaml에서는 oracle,oracle-local을 켜 실제 PC의 Oracle 설정을 읽습니다.'),
T(['설정','왜 필요한가'],[['max-file-size=10MB','한 파일 요청이 너무 커지지 않도록 기본 제한'],['max-request-size=12MB','파일과 폼을 합친 전체 요청 제한'],['ddl-auto=validate','실제 DB 구조와 엔티티가 맞는지만 확인'],['open-in-view=false','화면을 만드는 동안 DB 연결에 계속 기대지 않게 함'],['session.timeout=30m','사용하지 않는 로그인 세션의 만료 기준'],['cookie.http-only=true','브라우저 JavaScript가 세션 쿠키를 읽지 못하게 함'],['bootstrap.enabled=false','기존 DB에서 초기 계정을 다시 만들지 않음']]),
P('프로필 사진은 기본 파일 제한 10MB보다 더 엄격한 5MB 제한을 서비스에서도 확인합니다. 설정의 큰 문을 통과했다고 업무별 작은 문까지 통과한 것은 아닙니다. 실제 비밀번호는 HTML 학습서에 복사하지 않습니다. ${환경변수:기본값}은 실행 장소에서 받은 값을 먼저 사용하고 없으면 기본값을 쓴다는 표시입니다.'),
Q('validate는 테이블을 자동으로 만들어 주나요?','아니요. 이미 있는 테이블이 엔티티와 맞는지 확인합니다. 새 Oracle DB는 생성 SQL을, 기존 DB의 구조 변경은 마이그레이션 SQL을 적용해야 합니다.')]},
{id:'db',title:'08. DB는 오래 보관하는 표입니다',tag:'테이블·키·관계',blocks:[
P('프로그램 메모리에만 넣어 두면 서버를 끌 때 사라질 수 있습니다. DB는 다시 켜도 기록을 찾을 수 있게 보관합니다. 테이블은 표, 행은 한 건, 컬럼은 한 항목입니다. 직원 테이블의 한 행은 직원 한 명이며 name, department, role 같은 항목이 있습니다.'),
C(J+'domain/BaseEntity.java','@MappedSuperclass',8,['이 부모 클래스의 필드를 자식 엔티티의 테이블에 포함합니다. BaseEntity 자체의 별도 테이블을 만드는 표시가 아닙니다.','직접 만들기보다는 여러 엔티티가 물려받을 공통 설계도입니다.','id는 기본키이며 시퀀스로 값을 받습니다. 기본키는 행을 구분하는 고유 번호입니다.','portal_seq를 공통 번호표 기계로 사용하고 한 번에 1개씩 번호를 배정합니다.','DB 번호를 Long으로 저장합니다. 저장 전에는 null일 수 있습니다.','version은 동시 변경 확인용 숫자입니다. 수정 시 JPA가 버전을 확인하고 올립니다.','클래스의 끝입니다.']),
P('외래키 FK는 다른 표의 번호표를 가리킵니다. Attendance의 employee는 “이 출근 기록이 누구 것인지”를 직원 ID로 연결합니다. @ManyToOne은 여러 출근 기록이 한 직원을 가리킬 수 있다는 뜻입니다. 직원 이름을 각 출근 행에 복사해 저장하면 이름을 바꿀 때 전부 수정해야 하므로 보통 번호로 연결합니다.'),
P('현재 DB는 테이블 19개, 컬럼 202개입니다. 모든 관계가 물리 FK는 아닙니다. 파일의 owner_type + owner_id, 승인 변경의 request_type + request_id, 직원의 profile_file_id는 서비스에서 의미를 관리하는 논리 참조가 있습니다. 선이 그려져 있다는 이유만으로 DB가 모든 연결을 강제한다고 생각하면 안 됩니다.'),
Q('직원 ID가 7이고 다음 ID가 9여도 고장인가요?','아닙니다. 여러 테이블이 공통 시퀀스를 쓰고, 취소된 트랜잭션 등으로 번호가 건너뛸 수도 있습니다. ID는 개수를 세는 숫자가 아니라 중복 없이 대상을 구별하는 번호입니다.')]},
{id:'entity',title:'09. Entity와 Repository가 표를 코드에 연결합니다',tag:'JPA와 조회',blocks:[
C(J+'domain/Employee.java','@Entity',11),
P('@Entity는 DB에 저장할 객체라는 표시이고 @Table은 실제 표 이름과 고유 제약을 지정합니다. @Column(nullable=false)는 DB에서 이 칸을 비워 두지 말라는 뜻입니다. @Getter와 @Setter는 값을 꺼내고 넣는 메서드를 자동 생성합니다. 비밀번호는 원문이 아니라 해시 결과를 저장합니다.'),
C(J+'repository/EmployeeRepository.java','public interface',10),
P('Repository는 DB에 부탁하는 창구입니다. JpaRepository<Employee,Long>은 직원 객체를 Long ID로 다루는 기본 기능을 얻는다는 뜻입니다. findByLoginId는 메서드 이름을 읽어 loginId로 찾는 조회를 만듭니다. @Query가 있으면 이름 대신 개발자가 쓴 조회식이 기준입니다. JPQL은 테이블명보다 Java 엔티티와 필드를 기준으로 적는 조회 언어입니다.'),
P('employees.save(e)는 직원 객체를 저장하도록 요청합니다. 반면 DB에서 꺼낸 객체의 값을 트랜잭션 안에서 바꾸면 save를 다시 쓰지 않아도 변경 감지로 수정될 수 있습니다. 아무 객체나 setter를 호출하면 DB가 바뀌는 것은 아닙니다. JPA가 관리 중인 객체인지, 변경 가능한 트랜잭션인지가 중요합니다.'),
Q('findAll().stream().filter(...)와 DB 조건 조회는 같나요?','최종 목록은 같을 수 있지만 과정은 다릅니다. findAll 뒤 필터링은 먼저 많은 행을 읽고 Java에서 거릅니다. 작은 프로젝트에서는 편하지만 데이터가 많아지면 DB 조건 조회와 페이징으로 개선할 수 있습니다.')]},
{id:'security',title:'10. 로그인 문지기는 SecurityConfig입니다',tag:'인증과 인가',blocks:[
P('인증은 “너 누구니?”이고 인가는 “그 일을 해도 되니?”입니다. 학생증 확인과 교무실 출입 허락이 다른 것처럼, 로그인했다고 모든 자료를 볼 수 있는 것은 아닙니다.'),
C(J+'config/SecurityConfig.java','@Bean UserDetailsService',7),
P('사용자가 로그인 ID와 비밀번호를 제출하면 Spring Security가 이 UserDetailsService로 계정을 찾습니다. 삭제되거나 비활성인 계정은 disabled로 표시합니다. BCryptPasswordEncoder는 비밀번호를 복원하는 암호화 도구가 아니라 비교용 해시를 만드는 도구입니다. 같은 비밀번호도 해시 문자열이 늘 같을 필요는 없으며 matches로 검증합니다.'),
C(J+'config/SecurityConfig.java','return http.authorizeHttpRequests',11),
P('주소별 규칙은 먼저 맞는 항목이 중요합니다. /admin/**는 ADMIN, /employee/**와 /manager/**는 EMPLOYEE 역할을 요구합니다. /manager/**에 직원 역할이 있다고 해서 모든 직원이 부서 관리자가 되는 것은 아닙니다. DepartmentAccess가 부서 관리 권한과 소속 부서를 다시 확인합니다.'),
P('로그인 성공 뒤 서버는 세션에 로그인 상태를 기억하고 브라우저는 세션을 식별하는 쿠키를 보냅니다. 비밀번호를 모든 클릭마다 다시 보내는 방식이 아닙니다. CSRF 토큰은 로그인 쿠키만 믿고 외부 페이지의 요청을 실행하지 않도록, 서버가 준비한 폼에서 나온 요청인지 확인하는 추가 장치입니다.'),
Q('관리자 메뉴를 숨기면 권한 처리가 끝날까요?','아니요. 사용자가 주소를 직접 호출할 수 있습니다. 화면에서 숨기는 것과 서버에서 거부하는 것을 함께 해야 합니다.')]},
{id:'mvc',title:'11. Controller가 화면에 넣을 재료를 준비합니다',tag:'MVC와 Thymeleaf',blocks:[
C(J+'web/PortalController.java','@GetMapping("/login")',1,['GET /login 요청이 오면 login 메서드를 실행하고 "login"이라는 화면 이름을 돌려줍니다. Thymeleaf가 templates/login.html을 찾아 실제 HTML을 만듭니다. 이 한 줄에는 주소 연결, 메서드 선언, return이 함께 들어 있습니다.']),
P('Model은 화면에 전달할 도시락입니다. model.addAttribute("me",me)는 me라는 이름표를 붙여 현재 직원을 담습니다. 화면의 ${me.name}은 그 도시락에서 직원 이름을 꺼냅니다. return "portal"은 글자 portal을 화면에 쓰라는 뜻이 아니라 portal.html을 이용하라는 뜻입니다. @RestController의 반환은 다르며 보통 JSON 데이터가 됩니다.'),
C(J+'web/PortalController.java','model.addAttribute("managerView"',5),
T(['화면 표현','뜻'],[['th:text="${me.name}"','값을 안전한 글자로 표시'],['th:if="${admin}"','관리자일 때만 이 부분 생성'],['th:each="e : ${employees}"','직원 목록을 하나씩 반복'],['th:href="@{/employee/main}"','앱 주소를 링크에 넣기'],['th:action="@{/employee/profile}"','폼을 보낼 주소 만들기'],['th:replace="~{fragments/...}"','공통 화면 조각 끼워 넣기']]),
P('템플릿의 th:text는 값을 HTML 코드로 실행하지 않고 글자로 표시합니다. 사용자가 이름 칸에 태그를 넣더라도 태그가 실행되게 만들면 안 됩니다. JavaScript 채팅도 같은 이유로 innerHTML 대신 textContent를 사용합니다.'),
Q('Controller가 비밀번호를 화면에 담으면 될까요?','안 됩니다. 화면에 필요 없는 민감한 값은 응답에 넣지 않는 것이 원칙입니다. 채팅은 필요한 표시 정보만 DTO로 골라 반환합니다.')]},
{id:'checkin',title:'12. 출근 버튼 하나를 처음부터 끝까지 따라갑니다',tag:'핵심 실습',blocks:[
F('화면의 POST 폼','Security: 로그인·CSRF','PortalController.checkIn','PortalService.checkIn','AttendanceRepository → DB','대시보드 다시 조회'),
C(J+'web/PortalController.java','@PostMapping("/employee/check-in")',1,['주소가 POST /employee/check-in이면 이 메서드가 받습니다. Principal p에서 로그인한 사람의 이름을 꺼내 서비스에 전달합니다. 처리 후 done으로 대시보드에 다시 이동합니다. 사용자가 숨겨진 입력칸으로 보낸 직원 ID를 출근 주인으로 믿지 않습니다.']),
C(J+'service/PortalService.java','@Transactional public void checkIn',9,['이 메서드의 DB 작업을 한 묶음으로 처리합니다. 모두 성공하면 확정하고 문제가 나면 되돌리는 경계입니다.','로그인 ID로 직원을 찾고 해당 직원 행을 잠급니다. 동시에 두 번 출근하려는 요청이 순서를 지키게 합니다.','브라우저 시계 대신 서버의 Clock에서 현재 날짜·시간을 가져옵니다.','같은 직원·같은 날짜에 기록이 없는지 확인합니다. 이미 있으면 BusinessException으로 멈춥니다.','새 출근 객체에 직원, 근무일, 출근 시각을 넣습니다. 한 줄에 여러 setter가 있으므로 세미콜론마다 나누어 읽으세요.','현재 시각이 09:00보다 뒤인지 검사해서 지각 표시를 정합니다. 정확히 09:00이면 isAfter가 거짓입니다.','새 출근 객체를 저장하도록 요청합니다. SQL 실행과 최종 확정은 JPA·트랜잭션이 관리합니다.','출근 메서드가 끝납니다.','다음은 퇴근 메서드의 시작입니다. 출근과 같은 트랜잭션 원칙을 사용합니다.']),
P('퇴근은 오늘 출근 기록을 먼저 찾습니다. 기록이 없으면 “먼저 출근 체크” 안내를 내고, 이미 퇴근 시각이 있으면 다시 덮어쓰지 않습니다. 퇴근 시각을 넣고 18:00 이전이면 조퇴 표시를 정합니다. 이 코드의 시간 기준은 회사 법규 전체를 구현한 것이 아니라 현재 프로젝트의 업무 규칙입니다.'),
P('POST 처리 뒤 redirect하는 방식을 PRG라고 부릅니다. 제출 후 결과를 GET으로 다시 읽게 하면 결과 화면 새로고침이 같은 제출의 반복이 되는 일을 줄일 수 있습니다. 하지만 이것만으로 동시 중복은 막을 수 없으므로 잠금과 DB 유일 제약도 필요합니다.'),Q('브라우저 시계를 8시로 돌리면 지각을 피할 수 있나요?','출근 시각은 서버 Clock에서 가져오므로 브라우저 시계만 바꾸어 출근 시간을 정할 수 없습니다.')]},
{id:'transactions',title:'13. 두 사람이 동시에 누르면 어떻게 될까요?',tag:'트랜잭션·잠금·버전',blocks:[
P('트랜잭션은 “묶음으로 성공하거나 실패하는 작업”입니다. 친구에게 구슬을 3개 주는데 내 구슬만 줄고 친구 구슬은 늘지 않으면 안 되겠죠. 여러 변경을 하나의 묶음으로 다루는 이유입니다. @Transactional(readOnly=true)는 조회 중심 기본값이고, 변경 메서드에는 별도 @Transactional이 붙습니다.'),
P('비관적 잠금은 한 사람이 장부를 고치는 동안 다른 사람을 잠깐 기다리게 하는 방식입니다. @Lock(PESSIMISTIC_WRITE)로 가져온 직원이나 자원 행을 기준으로 관련 작업 순서를 맞춥니다. “행 잠금이 모든 종류의 경쟁을 자동 해결한다”는 뜻은 아닙니다. 관련 코드가 같은 잠금 대상을 같은 순서로 사용해야 합니다.'),
C(J+'repository/EmployeeRepository.java','@Lock',4),
P('낙관적 잠금은 장부에 수정 번호를 붙여 두는 방식입니다. 3번 버전을 읽은 뒤 누군가 4번으로 바꿨다면, 내가 3번 기준으로 덮어쓰는 것을 감지합니다. @Version이 그 번호입니다. 정정 신청에는 별도로 신청 당시 원본 버전과 시각도 저장해 승인 시 비교합니다.'),
P('중요한 세부사항도 있습니다. Spring의 트랜잭션은 보통 프록시라는 중간 담당자를 통해 메서드에 들어갈 때 작동합니다. 같은 객체가 자기 메서드를 직접 부르는 경우 새 경계가 자동으로 생기지 않습니다. 이 프로젝트는 변경 흐름의 바깥 서비스 메서드가 이미 트랜잭션 안에서 실행되도록 사용합니다.'),
Q('직원 행을 잠가도 DB 고유 제약을 두는 이유는 무엇일까요?','같은 규칙을 지키지 않는 다른 경로나 실수까지 고려해 DB에 마지막 방어선을 두는 것입니다. 잠금은 처리 순서, 고유 제약은 저장 가능한 데이터 모양을 지킵니다.')]},
{id:'leave',title:'14. 휴가를 0.5일 단위로 계산하는 이유',tag:'신청과 상태',blocks:[
P('연차는 하루, 반차는 반나절입니다. 이 프로젝트는 0.5일을 1단위로 세어 정수로 계산합니다. 15일은 30단위, 하루는 2단위, 반차는 1단위입니다. 병가는 여기서는 차감 0으로 정했습니다. 이것은 보편적인 모든 회사 규정이 아니라 프로젝트 규칙입니다.'),
C(J+'service/PortalService.java','int units = kind',5),
P('신청은 결재자, 날짜 순서, 연도, 실제 근무일, 기존 신청과의 겹침, 잔여 연차를 차례로 확인합니다. 같은 날짜의 오전 반차와 오후 반차는 서로 보완하는 경우로 허용합니다. 아직 승인 전이라도 PENDING이면 연차를 예약해서, 여러 신청으로 잔여량을 넘기는 것을 막습니다.'),
T(['상태','뜻','연차 합계에 포함?'],[['PENDING','승인 대기','예'],['APPROVED','승인됨','예'],['REJECTED','반려됨','아니오'],['CANCELLED','취소됨','아니오']]),
P('직원은 대기 신청을 취소할 수 있고, 부서 결재자는 승인하거나 반려할 수 있습니다. 이미 승인된 내용의 취소·변경은 별도 요청을 거칩니다. 화면의 버튼 하나가 아니라 상태에 따라 가능한 길이 달라지는 “상태 흐름”을 설계한 것입니다.'),Q('15일 중 승인 2일, 대기 반차 1건이면 남은 연차는 얼마인가요?','30단위 − 4단위 − 1단위 = 25단위, 즉 12.5일입니다. 대기 반차가 반려되면 다시 13일이 됩니다.')]},
{id:'holiday',title:'15. 쉬는 날은 달력 규칙으로 모읍니다',tag:'공휴일·근무일',blocks:[
C(J+'service/WorkCalendarService.java','public Set<LocalDate> closedDates',3,['등록된 휴무일에서 날짜만 꺼내 Set으로 모읍니다. 같은 날짜를 빠르게 있는지 확인할 수 있는 꾸러미입니다.','토요일도 아니고 일요일도 아니고 휴무일 목록에도 없는 날만 근무일입니다. &&는 세 조건을 모두 만족해야 한다는 뜻입니다.','시작일부터 종료일을 포함하도록 end.plusDays(1)까지 날짜를 펼치고, 근무일만 세어 정수로 돌려줍니다. datesUntil의 끝은 포함되지 않아서 하루를 더합니다.']),
P('휴무일을 추가하면 앞으로 신청하는 휴가만 달라지는 것이 아닙니다. 기존 대기·승인 휴가의 차감 단위도 다시 계산합니다. 예를 들어 이미 신청한 수요일을 회사 휴무일로 바꾸면 해당 휴가의 차감량이 줄 수 있습니다. 그래서 휴무일 저장은 단순한 달력 글자 추가보다 영향이 큽니다.'),
P('공휴일 API를 자동 호출하는 코드는 없습니다. 관리자가 날짜와 이름을 직접 등록합니다. 자동 수집을 했다고 발표하면 코드와 맞지 않습니다. 출퇴근 기록은 지우지 않고 보고서에서 근무일·휴가 여부를 적용해 상태를 계산합니다.'),Q('월요일부터 일요일까지 중 금요일이 회사 휴무일이면 근무일은 며칠인가요?','토·일을 빼면 5일이고 금요일도 빼면 4일입니다. 등록된 휴무일을 포함하여 계산해야 합니다.')]},
{id:'overtime',title:'16. 초과근무는 이미 일한 시간을 신청합니다',tag:'시간 검증',blocks:[
C(J+'service/OvertimeService.java','var now=LocalDateTime.now(clock);',7),
P('신청일은 오늘 또는 과거이면서 입사일 이전이 아니어야 합니다. 종료 시각은 시작보다 뒤이고, 실제로 이미 지나간 시각이어야 합니다. “오늘”이라고 해서 오늘 밤 아직 오지 않은 23시를 신청할 수 있는 것은 아닙니다. 날짜와 시간을 합쳐 now와 비교하는 이유입니다.'),
P('두 구간이 겹치는지 보는 쉬운 공식은 “내 시작이 상대 끝보다 앞이고, 내 끝이 상대 시작보다 뒤인가?”입니다. 예약에도 같은 아이디어가 등장합니다. 18~19시와 19~20시는 경계만 맞닿아서 겹치지 않습니다. 자정을 넘는 근무는 현재 구현에서는 날짜를 나누어 신청하게 합니다.'),
N('승인된 초과근무 신청 시간과 실제 출퇴근으로 계산한 초과근무 통계는 구분합니다. 승인 시간까지 실제 근무시간에 더하면 중복 계산이 될 수 있습니다.'),Q('현재 17시인데 오늘 18~20시 초과근무를 신청하면 어떻게 되나요?','끝난 근무가 아니므로 서버에서 거부합니다. 달력에서 오늘을 선택할 수 있다는 것과 오늘의 미래 시간을 허용한다는 것은 다릅니다.')]},
{id:'correction',title:'17. 출퇴근 정정은 원본 사진을 찍어 둡니다',tag:'스냅샷과 충돌',blocks:[
P('출근 버튼을 깜빡했다면 정정을 신청합니다. 이때 바로 출근 기록을 바꾸지 않고, “지금 기록은 이렇고 이렇게 바꾸고 싶다”는 신청서를 따로 저장합니다. originalCheckIn·originalCheckOut은 원래 값, proposedCheckIn·proposedCheckOut은 요청한 값입니다. originalVersion은 원래 수정 번호입니다.'),
C(J+'service/AttendanceCorrectionService.java','var original=attendance.findBy',6),
P('승인할 때 다시 실제 기록을 읽어 신청 당시 값과 비교합니다. 신청 이후 다른 정정이나 퇴근 처리가 있었다면 지금 신청서로 덮어쓰면 안 됩니다. 버전과 시각이 맞는지 검사하고 다르면 반려 후 다시 신청하도록 안내합니다. 기존 기록이 아예 없었던 경우도 구별해야 해서 원본 버전이 null일 수 있습니다.'),
C(J+'service/AttendanceCorrectionService.java','if(approve) {',10),
Q('신청할 때 원래 출근이 09:30이었는데 승인 전에 09:10으로 바뀌었다면?','옛날 09:30을 기준으로 만든 신청서이므로 바로 덮어쓰지 않습니다. 원본이 달라졌다는 사실을 알려 새 상황에 맞게 처리하게 합니다.')]},
{id:'amend',title:'18. 승인된 것을 바꿀 때는 새 요청을 만듭니다',tag:'이력 보존',blocks:[
P('이미 도장을 받은 신청서를 직원이 직접 고치면 누가 무엇을 승인했는지 알 수 없게 됩니다. 그래서 RequestAmendment라는 별도 취소·변경 신청서를 사용합니다. requestType은 원본 종류, requestId는 원본 번호, originalSummary는 당시 내용을 읽기 좋게 적어 둔 요약입니다.'),
C(J+'service/AmendmentService.java','if(approve){',8),
P('승인 처리에서는 원본의 버전이 그대로인지 확인한 뒤 undo로 예전 승인을 취소하고, 변경이면 replace로 새 신청을 만들어 승인합니다. replacementId는 새 신청을 가리킵니다. 기존 신청의 행을 없애기보다 상태와 연결을 남겨 과거 흐름을 따라갈 수 있게 합니다.'),
P('출퇴근 정정의 취소는 특별합니다. 원래 기록이 있었다면 그 값으로 복원하고, 원래 기록이 없어서 정정 승인으로 새 기록을 만들었다면 그 실제 출퇴근 행을 없애 미기록 상태로 돌립니다. “계정·신청 이력을 보존한다”와 “어떤 테이블도 절대 삭제하지 않는다”는 같은 말이 아닙니다.'),Q('변경 중 새 신청 검증이 실패하면 이전 취소만 남아도 될까요?','안 됩니다. 한 트랜잭션으로 묶어 전체 변경이 실패하면 함께 되돌아가야 합니다. 그래야 원본만 취소되고 새 신청이 없는 중간 상태를 피할 수 있습니다.')]},
{id:'department',title:'19. 부장과 본부장도 자기 부서 규칙을 지킵니다',tag:'소속과 권한',blocks:[
C(J+'service/DepartmentAccess.java','public boolean isManager',5),
P('관리 권한자는 활성 직원이며 삭제되지 않았고 departmentManager가 true여야 합니다. 부서명이 같아야 소속 부서로 판단합니다. 현재 DB에 별도 부서 테이블이 있는 것이 아니라 직원의 department 문자열을 사용합니다.'),
C(J+'service/DepartmentAccess.java','public boolean canReview',3,['결재 가능한지 판정하는 메서드의 시작입니다.','같은 부서의 다른 관리자인지 먼저 확인합니다. 지정 결재자가 더 이상 자격이 없거나, 내가 지정 결재자이거나, 내가 본부장이고 지정자는 본부장이 아니면 처리할 수 있습니다. 괄호를 먼저 읽고 ||로 연결된 세 경우를 나누어 읽으세요.','메서드 끝입니다.']),
P('본부장은 별도의 ROLE_DIVISION_HEAD가 아닙니다. EMPLOYEE 역할, departmentManager 권한, positionName이 본부장이라는 조건의 조합입니다. 자기 신청의 자기 승인은 막고 소속 부서 제한은 유지합니다. “직급이 높으니 사이트의 모든 데이터에 접근 가능”이라는 설계가 아닙니다.'),Q('부장이라는 글자만 직원 직급에 넣으면 결재 권한이 생기나요?','아니요. 별도 부서 관리 권한이 부여되어야 합니다. 직급은 표시 정보이면서 일부 규칙의 재료이고, 권한 설정과 완전히 같은 것은 아닙니다.')]},
{id:'reports',title:'20. 근태 통계와 부서 캘린더는 계산해서 만듭니다',tag:'집계와 출력',blocks:[
P('근무시간·결근·잔여 연차 같은 숫자를 모두 별도 컬럼으로 저장하면 원본이 바뀔 때 숫자가 서로 어긋나기 쉽습니다. 이 프로젝트는 실제 출퇴근과 승인 휴가, 근무일 규칙을 읽어 보고서를 계산합니다. 미퇴근 기록은 완료된 근무시간으로 계산하지 않습니다.'),
C(J+'service/AttendanceReportService.java','private long workedMinutes',12),
P('점심시간을 무조건 60분 빼는 것이 아니라 실제 근무 구간과 12~13시의 겹친 부분만 뺍니다. 12:30~18:00이면 점심과 겹치는 30분만 제외합니다. 하루 완료 근무가 480분을 넘으면 그 초과분을 통계로 표시합니다. 반차와 종일 휴가도 지각·결근 판정에 영향을 줍니다.'),
P('DepartmentReportService는 소속 부서의 직원들을 모아 이 계산기를 반복 사용합니다. 캘린더에는 승인 휴가와 업무 마감일을 날짜별 목록으로 넣습니다. 다른 직원 업무는 제목·담당자·상태 요약만 보여주고 상세 링크는 본인 업무에만 제공합니다.'),
P('AttendanceWorkbook은 .xlsx 파일을 직접 만듭니다. XLSX는 여러 XML 파일을 ZIP으로 묶은 형식입니다. 이 프로젝트는 숫자는 숫자로, 사용자 글자는 수식이 아닌 inline 문자열로 씁니다. 코드 안 일부 오래된 안내 문구에 “공휴일은 평일 처리”가 남아 있지만 현재 계산은 WorkCalendarService의 휴무일 규칙을 사용합니다. 설명문과 계산 코드는 분리해서 확인해야 합니다.'),Q('12:30~18:00 근무를 이 계산기로 계산하면 몇 분인가요?','전체 330분 중 점심 겹침 30분을 빼서 300분입니다. 실제 규정은 회사마다 다르지만 여기서는 이 규칙을 구현했습니다.')]},
{id:'tasks',title:'21. 개인 업무와 댓글은 나만 보는 작업 노트입니다',tag:'업무·일정',blocks:[
C(J+'service/GroupwareService.java','private boolean canTask',7),
P('현재 업무 관리에서는 직급과 관계없이 담당자로 본인만 선택합니다. canTask도 웹 관리자가 아니면서 업무 담당자 ID가 내 ID인지 확인합니다. 따라서 업무 댓글은 공용 게시판 댓글이 아니라 지금 구조에서는 개인 업무 진행 메모입니다. 팀원끼리 보는 댓글로 바꾸려면 화면만 바꾸는 것이 아니라 접근 규칙부터 다시 설계해야 합니다.'),
C(J+'service/GroupwareService.java','@Transactional public void comment',6),
P('여기에는 업무 작성자와 담당자에게 알림을 보내는 코드가 남아 있습니다. 하지만 현재 새 업무는 작성자와 담당자가 모두 본인이므로 자기 자신을 수신자 목록에서 빼면 상대방 알림이 없어질 수 있습니다. 코드에 notify가 있다고 무조건 다른 사람이 댓글을 볼 수 있다는 뜻은 아닙니다.'),
P('일정은 Schedule에 날짜·시각·색상·배정 여부를 저장합니다. 개인 일정과 부서 관리자의 배정 일정은 같은 테이블이지만 수정 가능한 사람의 규칙이 다릅니다. 월 달력은 첫날의 요일을 구하고 앞뒤 빈칸을 포함한 날짜 칸들을 만들어 그날의 일정을 묶어 넣습니다.'),Q('부서 캘린더에 내 업무 제목이 보이면 다른 사람이 댓글도 볼까요?','아니요. 캘린더 요약 공개와 업무 상세 접근은 다릅니다. 현재 댓글·본문·첨부는 본인 업무 상세에서만 접근합니다.')]},
{id:'approval',title:'22. 전자결재는 순서가 있는 도장 릴레이입니다',tag:'문서와 결재 단계',blocks:[
P('구매 요청, 지출결의, 출장 신청은 ApprovalDocument에 본문을 저장하고 ApprovalStep에 결재자별 순서를 저장합니다. 한 문서에 여러 단계가 있으므로 표를 둘로 나눕니다. 결재자 이름 5개를 한 칸에 쉼표로 넣는 방식보다 단계별 상태·처리 시각을 관리하기 쉽습니다.'),
C(J+'service/GroupwareService.java','int order=1;for(var e:reviewers)',2),
P('currentStep은 지금 도장을 찍을 차례입니다. 해당 순번의 결재자만 처리할 수 있습니다. 중간 승인이면 다음 순번으로 넘어가고, 마지막 승인이면 문서 전체가 APPROVED가 됩니다. 한 단계가 반려되면 문서 전체는 REJECTED가 됩니다.'),
C(J+'service/GroupwareService.java','step.setStatus(approve?',5),
P('문서 생성 때 1~5명의 서로 다른 결재자를 선택하고 자기 자신을 제외합니다. 결재자는 부서 관리 권한을 가져야 합니다. 작성자와 지정 결재자만 읽을 수 있고 웹 관리자에게 모든 문서를 열어 주지 않습니다. 휴가 결재의 소속 부서 조건과 전자결재 결재선 선택 규칙이 완전히 같지는 않으므로 해당 서비스의 검사를 직접 확인해야 합니다.'),
Q('첫 번째 결재자가 아직 처리하지 않았는데 두 번째 사람이 승인하면?','currentStep과 자신의 결재 순번이 일치하지 않으므로 처리 권한 검사에서 막혀야 합니다.')]},
{id:'reservations',title:'23. 회의실 예약은 겹치는 시간을 막습니다',tag:'자원과 예약',blocks:[
P('BookableResource는 대회의실 같은 물건이고 ResourceReservation은 그 물건을 쓰겠다는 한 건의 신청입니다. 물건과 신청을 한 표로 합치면 방 이름을 예약마다 복사해야 하므로 나눕니다. capacity는 설명용 수용 인원·수량이며 그 숫자만큼 동시 예약을 허용하는 로직은 아닙니다.'),
C(J+'service/GroupwareService.java','@Transactional public void reserve',8),
P('먼저 시간 순서와 미래 예약인지 확인합니다. 그다음 자원 행을 잠근 뒤 같은 자원의 PENDING 또는 ACTIVE 예약과 겹치는지 검사합니다. 기존 예약이 승인 대기라고 무시하면 여러 사람이 같은 시간에 신청해 충돌하므로 대기도 포함합니다. 통과하면 승인 대기 예약을 만들고 관리자에게 알립니다.'),
P('관리자는 승인 또는 반려하고 직원은 가능한 상태의 자기 예약을 취소합니다. 승인 후 상태는 ACTIVE입니다. 다른 신청에서 쓰는 APPROVED와 단어가 다릅니다. 여러 기능의 상태 이름을 무조건 하나로 외우지 말고 해당 엔티티와 서비스에서 확인하세요.'),Q('10~11시 예약이 있을 때 11~12시는 가능한가요?','경계만 닿으므로 시간 겹침 검사에서는 가능합니다. 10:30~11:30이면 겹치므로 거부합니다.')]},
{id:'files',title:'24. 자료실과 첨부파일은 읽을 권한도 확인합니다',tag:'공개 범위와 BLOB',blocks:[
P('자료실 문서는 SharedDocument에 제목·본문·공개 범위를 담습니다. COMPANY는 화면에 “모든 부서”로 표시하고 DEPARTMENT는 등록 부서를 기준으로 판단합니다. department는 등록 당시 부서 문자열이므로 작성자의 부서가 바뀌었다고 자동으로 바뀌는 외래키가 아닙니다.'),
C(J+'service/GroupwareService.java','public boolean canLibrary',3),
P('StoredFile은 파일 원본을 byte[]로 받고 Oracle BLOB에 보관합니다. BLOB은 큰 이진 데이터 상자입니다. ownerType은 TASK·DOCUMENT·LIBRARY·PROFILE 같은 종류, ownerId는 그 종류의 대상 번호입니다. 같은 숫자 10이라도 업무 10번과 자료 10번은 다르므로 종류가 함께 필요합니다.'),
C(J+'service/GroupwareService.java','public void fileAccess',8),
P('다운로드 주소를 알아도 먼저 첨부 대상에 접근할 권한이 있는지 확인합니다. 프런트에서 링크를 안 보여 주는 것만으로 끝내지 않습니다. 프로필 사진은 일반 첨부 다운로드 창구와 분리되어 있고 로그인 사용자에게 이미지를 제공하는 별도 컨트롤러를 사용합니다. S3에 자동 저장되는 구조가 아닙니다.'),Q('자료실 링크를 복사해서 다른 부서 직원에게 주면 읽을 수 있나요?','공개 범위가 등록 부서이고 다른 예외 권한도 없다면 서버에서 거부합니다. 링크를 아는 것과 읽을 권한이 있는 것은 다릅니다.')]},
{id:'chat-server',title:'25. 채팅 서버는 참여자와 메시지를 확인합니다',tag:'JSON API',blocks:[
P('채팅은 전체 HTML을 다시 받기보다 JSON이라는 데이터 묶음을 받습니다. Person에는 ID·이름·부서·직급·사진 주소처럼 필요한 표시 정보만 넣고, Message에는 본문·보낸 시각·읽은 시각 등을 넣습니다. 이러한 전송용 모양을 DTO라고 부릅니다. 직원 객체 전체를 그대로 응답하지 않는 이유입니다.'),
C(J+'web/ChatController.java','@RestController',9),
P('GET /api/chat/people은 대화할 사람을 찾고, /inbox는 최근 대화와 안 읽은 개수를 보여 줍니다. /threads/{id}의 id는 별도 대화방 번호가 아니라 상대 직원 ID입니다. 현재 별도 채팅방 테이블이 없고 발신자·수신자 두 사람의 메시지를 묶어 1:1 대화처럼 보여 줍니다.'),
C(J+'service/ChatService.java','@Transactional public Message send',17),
P('send는 로그인한 본인을 발신자로 결정하고 메시지 길이, UUID, 자기 자신에게 보내는지, 상대가 활성인지 확인합니다. (sender_id, client_id) 고유 제약이 있으므로 서버 저장 후 응답만 끊어진 상황에서 같은 요청을 다시 보내도 기존 결과를 돌려줄 수 있습니다. 다른 내용에 같은 UUID를 재사용하면 거부합니다.'),
P('history는 나→상대 또는 상대→나인 조건으로만 조회합니다. 관리자도 이 규칙을 우회하지 않습니다. 최근 50개를 먼저 보여 주고 before ID를 이용해 더 오래된 메시지를 읽습니다. 51개를 조회해 50개를 표시하면 다음 페이지가 있는지 알 수 있습니다.'),Q('다른 사람 직원 ID 두 개를 요청에 넣으면 그 사람들 대화를 읽을 수 있나요?','API는 내 ID를 요청 값에서 받지 않고 인증된 계정에서 구합니다. 따라서 한쪽 참여자는 항상 로그인한 나로 고정됩니다.')]},
{id:'chat-js',title:'26. 채팅창 JavaScript는 화면 상태를 기억합니다',tag:'비동기·폴링·DOM',blocks:[
P('DOM은 브라우저가 화면 요소를 나무 모양으로 정리한 것입니다. querySelector로 요소를 찾고 addEventListener로 클릭·입력을 기다립니다. async/await는 서버 응답을 기다리는 동안 브라우저 전체를 멈추지 않게 작업을 나누는 표현입니다. Java의 스레드나 DB 트랜잭션과 같은 개념은 아닙니다.'),
C(R+'static/js/chat.js','async function poll()',4,['새 메시지를 확인하는 비동기 함수입니다.','이전 예약을 지웁니다. 이미 조회 중이면 겹쳐 조회하지 않습니다. 숨겨진 탭은 실제 API 요청 대신 나중 확인을 예약합니다.','대화 목록을 받고, 패널이 열렸고 대화 화면이면 메시지도 받습니다. 실패 안내 후 finally에서 상태를 풀고 닫힌 패널 10초, 열린 패널 4초 뒤 다시 실행합니다.','함수가 끝납니다. WebSocket 연결이 아니라 반복 HTTP 조회입니다.']),
P('mode는 목록·사람 찾기·대화 중 어디인지, peer는 현재 상대, messages는 ID별 메시지, drafts는 상대별 작성 중 글, retries는 재시도 UUID를 기억합니다. revision은 화면을 바꿀 때 증가하는 번호입니다. 느린 응답이 나중에 도착해도 요청 당시 화면 번호와 다르면 무시해서 다른 대화를 덮지 않게 합니다.'),
C(R+'static/js/chat.js','async function readVisible()',7),
P('읽음은 서버에서 목록을 가져왔다는 이유만으로 처리하지 않습니다. 패널이 열려 있고, 브라우저가 보이며, 메시지 상자가 화면에 실제로 겹치는지 확인합니다. 그래도 이것은 “사람이 내용을 이해했다”는 증명이 아니라 화면에 보였다는 기준입니다.'),
P('textContent는 받은 글을 글자로만 넣습니다. Enter는 전송, Shift+Enter는 줄바꿈입니다. 한글 입력 중 조합 이벤트는 아직 글자를 완성 중일 수 있어서 isComposing을 확인합니다. 전송 실패 때 글을 지우지 않고 같은 내용의 재시도에는 UUID를 유지합니다.'),Q('창이 열렸을 때 4초마다 확인하면 완전히 즉시 전달되나요?','아니요. 다음 조회까지 지연될 수 있습니다. 현재는 짧은 주기로 새 데이터를 확인하는 방식이며 WebSocket 실시간 통신이라고 설명하면 안 됩니다.')]},
{id:'photos',title:'27. 프로필 사진은 검사하고 작게 바꿔 저장합니다',tag:'파일 업로드',blocks:[
P('브라우저는 파일과 폼 내용을 multipart/form-data라는 포장으로 보냅니다. 서버는 MultipartFile로 받습니다. accept="image/jpeg,image/png"는 파일 선택 창의 안내일 뿐이므로 서버에서도 실제 이미지 형식을 검사합니다. 파일 이름이 .png라고 반드시 PNG인 것은 아닙니다.'),
C(J+'service/ProfileImageService.java','private byte[] normalize',17),
P('크기가 5MB 이하인지, 실제 JPEG 또는 PNG인지, 가로×세로가 1,600만 화소 이하인지 확인합니다. 압축된 파일이 작아도 풀면 너무 큰 이미지일 수 있으므로 픽셀 수도 확인합니다. 가운데 정사각형을 골라 256×256 PNG로 다시 그립니다. 원본의 메타데이터를 복사하지 않고 새 이미지 픽셀만 저장하는 방식입니다.'),
C(J+'service/ProfileImageService.java','@Transactional public void upload',9),
P('사진은 기존 portal_file에 PROFILE 종류로 저장하고 직원의 profileFileId를 새 파일 ID로 바꿉니다. 이전 사진이 있으면 같은 변경 트랜잭션 안에서 지웁니다. 직원 엔티티에 이미지 바이트를 직접 넣지 않아 직원 목록을 읽을 때 큰 사진 데이터까지 항상 끌고 오지 않게 합니다.'),
P('화면은 사진 URL이 있으면 img를 올리고 없으면 이름 첫 글자를 보여 줍니다. profile.js는 이미지 로딩 실패 시 img만 제거해서 뒤에 있던 첫 글자가 보이게 합니다. 사진을 바꿀 권한은 본인 또는 웹 관리자에게 있고, 조회는 로그인 사용자에게 허용합니다.'),Q('사진 확장자만 검사하면 왜 부족할까요?','이름은 마음대로 바꿀 수 있기 때문입니다. 서버가 실제 데이터를 이미지로 읽을 수 있는지, 허용 형식인지 확인해야 합니다.')]},
{id:'frontend',title:'28. HTML·CSS·JavaScript는 역할이 다릅니다',tag:'화면 구조·스타일·행동',blocks:[
P('HTML은 책상·의자·칠판을 어디에 놓을지 정하는 구조, CSS는 색과 크기·간격을 정하는 꾸미기, JavaScript는 버튼을 누르면 창을 여는 행동입니다. 세 가지를 함께 쓰되, 업무 권한의 최종 판단은 서버에 남겨 둡니다.'),
T(['파일','주요 책임'],[['portal.html','상단바·메뉴·현재 페이지 조합'],['fragments/employees.html','직원 목록과 등록·수정 패널'],['employees.js','패널 열기·탭·필터·10명 단위 페이지'],['calendar.js','일정 대화상자와 날짜별 입력'],['notifications.js','알림 열기·읽음 처리 후 이동'],['account-delete.js','삭제 확인과 정확한 대상 확인'],['chat.css','고정 버튼·대화 패널·모바일 크기'],['navigation.svg','각 상위 메뉴의 벡터 아이콘']]),
P('fixed는 화면을 스크롤해도 브라우저 창의 지정 위치에 머무는 배치입니다. 채팅 아이콘에 사용합니다. sticky는 스크롤하다 정해진 경계에서 붙는 방식이라 완전히 같은 것은 아닙니다. 사용자가 말한 “sticky 느낌”을 실제로는 fixed로 구현했습니다.'),
P('CSS의 gap·padding·margin은 각각 요소 사이, 안쪽 여백, 바깥 여백을 다룹니다. display:flex나 grid는 배치를 도와줍니다. @media는 화면 너비 같은 조건에 맞춰 다른 스타일을 적용합니다. 모바일에서 표는 내용까지 억지로 줄이기보다 표 영역 안에서 가로 스크롤하게 할 수도 있습니다.'),
P('dialog.showModal()은 모달 대화상자를 엽니다. 직원 패널은 오른쪽에서 나오지만 웹페이지의 dialog 요소입니다. aria-label, aria-expanded, 키보드 이동, Escape 닫기 같은 속성과 동작은 마우스 외 방식으로 사용하는 사람에게도 상태를 알려 줍니다.'),Q('CSS에서 관리자 버튼을 display:none으로 숨기면 보안이 해결되나요?','아니요. 화면의 모양만 바뀝니다. 요청을 직접 보내도 서버가 권한을 검사해야 합니다.')]},
{id:'notifications',title:'29. 알림은 어떤 페이지로 갈지도 알아야 합니다',tag:'비동기 요청과 안내',blocks:[
P('Notification은 누가 받을지, 무슨 말인지, 읽었는지, 어디로 이동할지를 저장합니다. targetPath가 있는 새 알림은 그 주소를 사용하고, 예전 알림은 NotificationLinks의 메시지 규칙으로 목적지를 추정합니다. 모든 알림이 같은 목록 화면으로 가면 사용자가 다시 찾아야 하므로 업무 화면으로 연결했습니다.'),
C(R+'static/js/notifications.js','const markRead = async form',13),
P('읽음 버튼을 누르면 기본 폼 이동 대신 fetch로 POST를 보냅니다. 서버가 204를 돌려주면 본문 없는 성공이라는 뜻입니다. 그때 목록의 unread 표시와 숫자를 바꿉니다. 실패했는데 먼저 숫자를 줄여 버리면 실제 상태와 달라지므로 성공 후 화면을 바꿉니다.'),
P('알림 제목 링크를 클릭할 때도 읽음 처리에 성공한 뒤 이동하도록 만들었습니다. 다만 새 탭으로 열기 같은 보조 키 조합은 브라우저의 원래 링크 동작을 존중합니다. API가 데이터를 바꾸면 읽음 대상이 현재 로그인 사용자의 알림인지 서버에서도 검사합니다.'),Q('채팅의 readAt과 알림의 readFlag는 왜 다른 모양일까요?','채팅은 읽은 시각도 표시할 수 있어 날짜·시간을 저장하고, 일반 알림은 현재 읽음 여부를 boolean으로 저장합니다. 필요한 정보에 맞춰 필드가 달라집니다.')]},
{id:'accounts',title:'30. 직원 삭제는 과거 기록을 없애지 않습니다',tag:'논리 삭제와 세션',blocks:[
P('직원을 DB에서 완전히 지우면 그 직원이 신청했던 휴가·작성했던 문서가 누구 것인지 연결이 깨질 수 있습니다. 현재 삭제는 deletedAt·deletedByLogin을 남기고 active와 관리 권한을 끄는 논리 삭제입니다. 목록에서는 숨기지만 업무의 과거 주인은 남아 있습니다.'),
C(J+'service/PortalService.java','@Transactional public String deleteEmployee',17),
P('자기 계정 삭제와 마지막 활성 관리자 삭제를 막습니다. 삭제할 로그인 ID를 정확히 입력하게 하고, 관련 대기 휴가·초과근무·정정 신청을 취소합니다. 계정 비밀번호도 무작위 값의 해시로 바꾸며 컨트롤러는 세션 레지스트리에서 기존 세션을 만료시킵니다. 삭제 후 기존 로그인 ID 재사용·복원 기능은 현재 제공하지 않습니다.'),
P('BootstrapData는 앱 시작 때 직원 수가 0인지 먼저 봅니다. 처음 빈 DB에서만 초기 계정을 만드는 도우미입니다. 서버 재시작 때마다 비밀번호를 덮어쓰는 코드가 아닙니다. 운영 DB에서 bootstrap.enabled=false를 두는 이유도 이해할 수 있습니다.'),Q('퇴사자의 과거 출근 기록을 보존하려면 이름만 따로 복사하면 될까요?','일부 이력 설계에서는 스냅샷을 쓰기도 하지만 현재 프로젝트는 직원 행을 보존하는 논리 삭제를 사용합니다. 과거 기록과 권한 차단을 함께 고려해야 합니다.')]},
{id:'errors',title:'31. 오류도 사용자가 이해할 수 있게 돌려줍니다',tag:'예외와 응답',blocks:[
P('오류에는 프로그램 버그뿐 아니라 정상적으로 막아야 하는 행동도 있습니다. “이미 출근했습니다”, “본인의 신청만 취소할 수 있습니다”처럼 업무 규칙 위반을 BusinessException으로 알려 줍니다. require는 조건이 거짓일 때 이런 예외를 던지는 작은 도우미입니다.'),
C(J+'web/PortalExceptionHandler.java','@ControllerAdvice',13),
P('@ControllerAdvice는 여러 컨트롤러에서 생기는 공통 오류를 한 곳에서 처리합니다. 잘못된 입력은 400, 충돌은 409, 너무 큰 업로드는 413으로 응답합니다. 채팅 API는 화면 HTML보다 message라는 JSON 값을 받아야 하므로 ChatController 안에 별도 처리기가 있습니다.'),
P('프로필 업로드는 BusinessException을 잡아 redirect와 flash 오류 메시지로 돌려줍니다. flash는 한 번 이동한 다음 화면에 안내를 보여 주기 위한 임시 메모입니다. 어떤 오류를 어떻게 보여 줄지는 일반 페이지와 비동기 API의 사용 방식에 맞춰야 합니다.'),Q('서버 오류 내용을 사용자에게 전부 그대로 보여주면 좋을까요?','아니요. 내부 구조나 연결 정보가 섞일 수 있고 사용자가 해결하기도 어렵습니다. 사용자에게 필요한 안내와 개발자가 확인할 기록을 구분합니다.')]},
{id:'tests',title:'32. 테스트는 “될 일”과 “막을 일”을 함께 확인합니다',tag:'검증',blocks:[
P('테스트는 코드를 대신 믿는 것이 아니라 반복 확인을 도와주는 시험지입니다. 정상 신청이 저장되는지만 확인하면 부족합니다. 남의 대화 읽기, 중복 전송, 미래 초과근무, 잘못된 사진, CSRF 없는 요청 같은 금지 행동도 막히는지 확인해야 합니다.'),
C('src/test/java/org/example/portal/ProfileImageTests.java','@Test void otherEmployeesCannot',9),
T(['도구·표현','하는 일'],[['@SpringBootTest','Spring 앱의 부품들을 함께 켜서 확인'],['@ActiveProfiles("test")','테스트용 설정 선택'],['MockMvc','실제 브라우저 없이 HTTP 요청 흐름 검사'],['with(user(...))','이 사용자가 로그인한 상황 만들기'],['with(csrf())','정상 CSRF 토큰이 있는 요청 만들기'],['assertThat / andExpect','예상 결과와 실제 결과 비교'],['@Transactional 테스트','일반적으로 테스트 뒤 DB 변경을 롤백해 격리']]),
P('현재 기록에는 자동 테스트 66개 통과가 있습니다. 자동 테스트는 H2에서 실행했고, 실제 Oracle의 스키마 검증·로그인·조회는 별도로 확인했습니다. 브라우저에서는 채팅 두 계정 간 송수신, 재시도, 사진 업로드·삭제, 모바일 배치를 확인했습니다. H2 테스트 통과가 Oracle의 모든 동작이나 대규모 부하를 보장하는 것은 아닙니다.'),
Q('테스트가 66개면 버그가 절대 없나요?','아니요. 테스트한 조건에서 예상대로 동작한다는 근거입니다. 테스트하지 않은 조합, 실제 네트워크 지연, DB별 차이, 많은 동시 사용자 상황은 별도의 확인이 필요합니다.')]},
{id:'docker',title:'33. Docker는 실행 환경을 상자에 포장합니다',tag:'빌드와 실행',blocks:[
C('Dockerfile','FROM maven',13,['Maven과 Java 17이 있는 빌드용 작업장을 준비합니다. build는 이 단계의 별명입니다.','작업 폴더를 /build로 정합니다.','준비물 목록 pom.xml을 복사합니다.','소스 폴더를 복사합니다.','Maven으로 JAR를 만듭니다. -DskipTests는 여기서 테스트 실행을 생략한다는 뜻이므로 테스트는 별도로 합니다.','읽기 편한 빈 줄입니다.','최종 실행에는 Maven 전체 대신 Java 실행 환경만 사용합니다. 다단계 빌드입니다.','최종 앱 작업 폴더를 /app으로 정합니다.','설정 폴더를 만들고 일반 실행 사용자에게 소유권을 줍니다.','앞 빌드 단계의 JAR만 최종 이미지로 가져옵니다.','루트 대신 지정한 사용자 번호로 실행합니다.','앱이 8080 포트를 사용한다는 정보를 남깁니다. 이것만으로 외부 포트가 열리지는 않습니다.','컨테이너 시작 때 java -jar /app/portal.jar를 실행합니다.']),
P('이미지는 실행 환경 설계가 담긴 포장이고 컨테이너는 그 포장을 실제로 실행한 것입니다. 소스 파일을 바꿨다고 이미 만들어 둔 JAR나 이미지가 자동으로 바뀌지 않습니다. 로컬 Docker에 새 코드가 보이게 하려면 새 이미지를 빌드하고 그 이미지로 컨테이너를 다시 만들어야 합니다.'),
C('compose.yaml','services:',14),
P('127.0.0.1:8080:8080은 PC의 로컬 접속 8080을 컨테이너 8080에 연결합니다. DB는 컨테이너 안이 아니라 Windows PC의 Oracle입니다. 그래서 host.docker.internal을 사용합니다. 설정 파일은 읽기 전용 마운트로 연결하고 이미지 안에 비밀번호 파일을 복사하지 않습니다.'),
Q('컨테이너 안의 localhost는 Windows PC인가요?','아니요. 컨테이너 자기 자신입니다. 이 로컬 구성에서는 host.docker.internal을 통해 PC에 접근합니다.')]},
{id:'aws',title:'34. AWS 테스트는 웹서버를 밖으로 옮긴 것입니다',tag:'EC2·HTTPS·터널',blocks:[
P('EC2는 인터넷에서 사용할 수 있는 가상 컴퓨터입니다. 이번 테스트는 EC2에 Spring Boot와 Caddy를 Docker로 실행하고, 데이터는 PC의 기존 Oracle에 두었습니다. Caddy는 HTTPS 요청을 받고 내부 앱으로 전달하는 역방향 프록시 역할을 합니다.'),
F('외부 브라우저 HTTPS','EC2의 Caddy','EC2의 Spring Boot','역방향 SSH 터널','PC의 Oracle'),
P('SSH 터널은 DB 포트를 아무에게나 열지 않고 EC2의 로컬 포트에서 PC DB로 이어지는 통로를 만든 것입니다. 웹서버가 밖에 있어도 PC와 Oracle과 터널이 켜져 있어야 DB를 사용할 수 있습니다. 그래서 이것은 PC에 의존하는 테스트 구성이지 독립적인 상시 운영 완성 구조는 아닙니다.'),
P('RDS는 관리형 DB 서비스이고 S3는 객체 파일 저장 서비스입니다. 이번 구성에서는 둘 다 사용하지 않았습니다. 신청 내역·채팅·파일은 Oracle에 저장합니다. AWS 접속 테스트는 종료했고, 그 이후 수정 기능은 로컬 서버에 먼저 적용했습니다. Vercel 관련 파일은 필요 없어 프로젝트에서 제거했습니다.'),
Q('EC2만 켜 두고 PC를 끄면 현재 테스트 구성이 그대로 동작할까요?','Oracle과 SSH 통로가 PC에 의존하므로 DB를 이용하는 기능이 정상 동작하지 않습니다. 독립 운영하려면 DB 위치·백업·접근 구성을 다시 마련해야 합니다.')]},
{id:'rebuild',title:'35. 같은 프로젝트를 다시 만든다면 이 순서로 연습하세요',tag:'학습 로드맵',blocks:[
L('1단계: main과 pom.xml로 서버를 켜고 GET /login에 간단한 화면을 띄웁니다.','2단계: Employee·BaseEntity·Repository를 만들고 H2에서 직원 한 명을 저장·조회합니다.','3단계: SecurityConfig로 로그인과 직원·관리자 주소를 나눕니다.','4단계: 출근·퇴근을 만들며 Controller → Service → Repository 흐름을 익힙니다.','5단계: 휴가·결재를 만들고 상태·소유권·중복·트랜잭션을 확인합니다.','6단계: 정정·승인 변경을 추가하며 원본 이력과 버전 충돌을 배웁니다.','7단계: 일정·부서 보고서·전자결재·예약·자료실을 같은 원칙으로 확장합니다.','8단계: JavaScript로 알림·직원 패널·채팅·사진을 연결합니다.','9단계: 금지 행동까지 테스트한 후 Docker와 Oracle을 연결합니다.','10단계: 배포 구성·HTTPS·외부 DB 연결·종료 방법까지 확인합니다.'),
P('연습할 때는 실제 Oracle 데이터를 대상으로 무작정 수정하지 말고 H2 테스트 환경을 사용하세요. 작은 변경 하나를 하고 관련 테스트를 돌려 결과를 확인하는 것이 좋습니다. 이 문서를 읽는 동안 실행되는 예제들은 설명용 계산이며 실제 포털 API를 호출하지 않습니다.'),
T(['연습 과제','먼저 볼 파일','완료 기준'],[['휴가 상태 한글 표시를 찾기','LeaveRequest.java','코드 값과 화면 글자의 차이를 설명'],['자기 업무만 보이는 이유 찾기','GroupwareService.canTask','관리자도 제외되는 조건을 말하기'],['사진 크기 검사 찾기','ProfileImageService.normalize','파일 용량과 픽셀 수를 구분'],['채팅 중복 전송 이유 설명','ChatService.send + chat.js','응답 유실 상황과 UUID를 그림으로 설명'],['서버 재시작 뒤 자료가 남는 이유','compose.yaml + Oracle 설정','이미지·컨테이너·DB를 구분']]),
Q('발표에서 “제가 이 구조를 이해했습니다”를 보여 주는 좋은 방법은?','기술 이름을 나열하기보다 한 버튼을 골라 요청 주소, 인증, 서비스 규칙, 저장, 결과 응답까지 직접 따라 설명해 보세요. 무엇을 막는지와 왜 그렇게 했는지도 말하면 좋습니다.')]},
{id:'glossary',title:'36. 헷갈릴 때 다시 보는 용어 사전',tag:'작은 사전',blocks:[
T(['단어','쉽게 풀이'],[['API','다른 프로그램이나 화면이 기능을 부탁하는 약속된 창구'],['Bean','Spring이 만들고 연결해 관리하는 객체'],['BCrypt','비밀번호를 비교용 해시로 만드는 방식'],['BLOB / CLOB','큰 바이트 데이터 / 큰 글자 데이터를 보관하는 DB 타입'],['CRUD','만들기·읽기·수정·삭제'],['CSRF','로그인 쿠키를 악용한 원치 않는 요청을 막기 위해 고려하는 공격'],['DTO','응답 등에 필요한 항목만 담은 전달용 객체'],['Entity','DB에 저장할 자료의 Java 모양'],['FK / PK / UK','다른 표 참조 / 기본 번호 / 중복 금지 조건'],['JAR','Java 실행 코드와 자원을 묶은 파일'],['JDBC','Java가 DB와 대화하는 연결 규약·도구'],['JPA','Java 객체를 DB에 연결하는 표준 규칙'],['Hibernate','JPA 규칙을 구현한 실제 도구'],['JSON','이름과 값으로 데이터를 표현하는 형식'],['ORM','객체와 관계형 DB를 연결하는 방식'],['PRG','POST 처리 뒤 Redirect하고 GET으로 결과 조회'],['Session','서버가 기억하는 로그인 등 사용자 상태'],['Transaction','관련 DB 변경을 하나의 성공·실패 묶음으로 처리'],['UUID','중복될 가능성이 매우 낮도록 만든 식별 문자열'],['Polling','정해진 간격으로 새 정보가 있는지 다시 요청'],['Migration','기존 DB를 다음 구조로 바꾸는 순서 있는 변경'],['Soft delete','기록을 지우는 대신 삭제 상태를 표시하는 방식'],['Snapshot','특정 순간의 값 복사본'],['서버 렌더링','서버가 데이터를 넣은 HTML을 만들어 전달'],['반응형','화면 크기에 따라 배치·크기를 조정']]),
P('이제 아래 코드 사전에서 원하는 파일을 골라 보세요. 원문은 읽기 전용으로 들어 있습니다. 한 줄에 여러 명령이 있는 곳은 오른쪽 설명을 함께 읽고, 관련 장으로 돌아오면 전체 의미를 연결할 수 있습니다. 이 학습서는 현재 파일의 스냅샷이므로 프로젝트를 바꾸면 다시 생성해야 같은 줄 번호가 됩니다.')]}];
